# 已出貨規劃紀錄

這裡收的是已經出貨（合併進 `main`）功能的規劃文件（plan／spec），
不是還在進行中的工作——現行規劃留在 `docs/superpowers/plans/` 與
`docs/superpowers/specs/`。

要查某份文件對應到哪個 commit，用：

```bash
git log --oneline -- <原路徑>
```

例如 `git log --oneline -- docs/superpowers/plans/2026-08-09-icd10-guide.md`。
