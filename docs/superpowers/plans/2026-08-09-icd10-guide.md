# ICD-10 門診導引頁面 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 產出單一離線 HTML 檔 `dist/icd10.html`：症狀導向 ICD-10-CM 選碼、內科/外科切換、慢性病與感染科快選、相關碼連鎖推薦、累積清單一鍵複製、全庫搜尋。

**Architecture:** 純 vanilla JS 單頁；健保署 ICD-10-CM 2023 中文版 CSV 經 Python 管線（fetch → convert → build）轉為 gzip+base64 內嵌資料；純邏輯抽在 `src/logic.js`（node 可測），UI 在 `src/app.js`；精選內容存 `src/curated/*.json`（Python 可驗證、build 時包成 `window.CURATED`）。

**Tech Stack:** Python 3.10+（stdlib only：csv/json/gzip/base64/urllib）、pytest、Node.js（`node --test` 測 logic.js）、Playwright（Python，E2E）、瀏覽器原生 `DecompressionStream`。

**Spec:** `docs/superpowers/specs/2026-08-09-icd10-guide-design.md`（已核可）

## Global Constraints

- UI 與所有文案一律繁體中文台灣用語；資料與程式碼禁止簡體字。
- 最終交付物是**單一檔案** `dist/icd10.html`：`file://` 雙擊可用、零外部請求、Edge/Chrome 80+。
- 資料來源固定：健保署官方 Excel「2023年中文版ICD-10-CM/PCS(正式版)(113.11.18更新)」，直接連結 `https://www.nhi.gov.tw/ch/dl-66644-25490863641c4e1889522442f8262b47-1.xlsx`（來源頁 `https://www.nhi.gov.tw/ch/cp-6071-469da-3051-1.html`），取 `ICD-10-CM` 工作表。
  - **2026-08-09 變更紀錄**：原定 data.gov.tw CSV API（rId=A21030000I-D20025-005）實測為 Big5 相容管線輸出，非 Big5 字元全部變成字面「?」（6,955 筆中文名受害，如「疱疹」→「?疹」），Task 3 資料品質關卡攔截後改用官方 xlsx；xlsx 僅殘餘 10 筆原始錯字（G72.41 内/体、I70.499 体、8 筆「瘻」缺字），由 convert.py 的 TYPO_FIXES 定點修正。
- 已驗證事實（xlsx 的 ICD-10-CM 工作表）：資料 96,802 列且代碼唯一；`USE=1`（可申報葉碼）73,681 筆；`USE=0`（類目）23,121 筆；欄位順序 `代碼,USE,英文名稱,中文名稱,狀態,修訂日期`（表頭首格為「2023年版\nICD-10-CM」含換行）。Task 1/2 的程式碼區塊為 CSV 版原文，實際以 Task 3 fix round 1 的 xlsx 版為準（見 ledger 與 repo 現行程式碼）。
- 精選碼（curated JSON 中出現的每一個碼）必須存在於全庫且 `USE=1`，由 `tests/test_curated.py` 機器驗證。
- 精選碼驗證失敗時：用 `python build/lookup.py <關鍵字>` 查全庫找正確碼替換，**不可**直接刪除條目（除非確認該概念不存在對應碼，並在 commit 訊息說明）。
- 所有 Python 讀寫檔案一律 `encoding='utf-8'`（讀原始 CSV 用 `utf-8-sig`），寫檔加 `newline='\n'`。
- 檔案輸出大小上限：`dist/icd10.html` ≤ 6 MB。
- Commit 訊息格式 `<type>: <繁中描述>`。

## 檔案結構總覽

```
ICD10/
├── build/
│   ├── fetch_data.py      # 下載健保署 CSV
│   ├── convert.py         # CSV → data/codes.min.json
│   ├── lookup.py          # 開發輔助：關鍵字查庫（驗證失敗時找碼用）
│   └── build.py           # 組裝 dist/icd10.html
├── src/
│   ├── template.html      # HTML 骨架＋CSS（含 %DATA% / %SCRIPTS% 佔位）
│   ├── logic.js           # 純邏輯（UMD：瀏覽器 window.ICDLogic ＋ node module.exports）
│   ├── app.js             # UI 綁定與狀態
│   └── curated/
│       ├── chronic.json         # [[code, label], ...]
│       ├── infectious.json      # [[code, label], ...]
│       ├── pathogens.json       # [[code, label], ...]（B95–B97、Z16）
│       ├── symptoms.json        # [{name, codes:[[code,label],...]}, ...]（內科面板）
│       ├── surgical_panels.json # 同 symptoms 格式（外科面板）
│       ├── surgical_quick.json  # [[code, label], ...]
│       └── related.json         # {code: [code, ...]}
├── data/                  # 原始 CSV 與 codes.min.json（gitignore）
├── dist/icd10.html        # 交付物（commit 進 repo）
├── tests/
│   ├── test_convert.py
│   ├── test_data_integrity.py
│   ├── test_curated.py
│   ├── logic.test.mjs
│   └── e2e_test.py
└── docs/superpowers/{specs,plans}/
```

---

### Task 1: 腳手架與資料下載腳本

**Files:**
- Create: `.gitignore`, `build/fetch_data.py`

**Interfaces:**
- Produces: `data/icd10cm_2023_zh.csv`（UTF-8+BOM、96,803 行含表頭）。後續 Task 2 讀取此檔。
- 注意：規劃階段已下載過此檔，腳本需冪等（檔案已存在且 >10MB 時跳過）。

- [ ] **Step 1: 寫 .gitignore**

```gitignore
data/
__pycache__/
.pytest_cache/
node_modules/
*.pyc
```

- [ ] **Step 2: 寫 build/fetch_data.py**

```python
"""下載健保署 2023年版中文 ICD-10-CM CSV 到 data/。已存在且大小合理則跳過。"""
import io, sys, urllib.request
from pathlib import Path

URL = "https://info.nhi.gov.tw/api/iode0000s01/Dataset?rId=A21030000I-D20025-005"
DEST = Path(__file__).resolve().parent.parent / "data" / "icd10cm_2023_zh.csv"
MIN_SIZE = 10 * 1024 * 1024  # 10MB：檔案實際約 13.7MB

def main():
    sys.stdout.reconfigure(encoding="utf-8")
    DEST.parent.mkdir(exist_ok=True)
    if DEST.exists() and DEST.stat().st_size > MIN_SIZE:
        print(f"已存在，跳過下載：{DEST} ({DEST.stat().st_size:,} bytes)")
        return
    req = urllib.request.Request(URL, headers={"User-Agent": "Mozilla/5.0"})
    with urllib.request.urlopen(req, timeout=120) as resp:
        raw = resp.read()
    if len(raw) < MIN_SIZE:
        raise SystemExit(f"下載檔案過小（{len(raw):,} bytes），來源可能異常")
    DEST.write_bytes(raw)
    print(f"下載完成：{DEST} ({len(raw):,} bytes)")

if __name__ == "__main__":
    main()
```

- [ ] **Step 3: 執行並驗證**

Run: `python build/fetch_data.py && python -c "import io; f=io.open('data/icd10cm_2023_zh.csv',encoding='utf-8-sig'); print(sum(1 for _ in f))"`
Expected: 印出「已存在，跳過下載」或「下載完成」；原始行數 `96804`（表頭＋96802 資料列＋Z53 列引號欄位內嵌 1 個換行；csv.reader 解析為 96803 列，Task 2 以 csv 解析數為準）

- [ ] **Step 4: Commit**

```bash
git add .gitignore build/fetch_data.py
git commit -m "feat: 資料下載腳本與 gitignore"
```

---

### Task 2: convert.py — CSV 轉 codes.min.json

**Files:**
- Create: `build/convert.py`
- Test: `tests/test_convert.py`

**Interfaces:**
- Produces: `data/codes.min.json` — JSON 陣列，每筆 `[code:str, use:0|1, en:str, zh:str]`，code 大寫含小數點（如 `"E11.9"`）。後續所有 Task 依賴此格式。
- Produces: `parse_rows(fileobj) -> list[list]`（供測試與 build.py import）。

- [ ] **Step 1: 寫失敗測試 tests/test_convert.py**

```python
import io, json, sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "build"))
from convert import parse_rows

SAMPLE = "﻿2023年版ICD-10-CM/PCS,USE,ICD-10-CM/PCS英文名稱,ICD-10-CM/PCS中文名稱,狀態,修訂日期\n" \
         "A00,0,Cholera,霍亂,,\n" \
         'A00.0,1,"Cholera due to Vibrio cholerae 01, biovar cholerae",血清型01 cholerae霍亂弧菌所致之霍亂,,\n' \
         "e11.9 ,1,Type 2 diabetes mellitus without complications,第2型糖尿病伴無併發症,代碼新增,2023/01/01\n" \
         ",,,,,\n"

def test_parse_rows():
    rows = parse_rows(io.StringIO(SAMPLE))
    assert rows == [
        ["A00", 0, "Cholera", "霍亂"],
        ["A00.0", 1, "Cholera due to Vibrio cholerae 01, biovar cholerae", "血清型01 cholerae霍亂弧菌所致之霍亂"],
        ["E11.9", 1, "Type 2 diabetes mellitus without complications", "第2型糖尿病伴無併發症"],
    ]  # BOM 剝除、引號逗號欄位完整、code 去空白轉大寫、空列跳過

def test_parse_rows_rejects_bad_header():
    import pytest
    with pytest.raises(AssertionError):
        parse_rows(io.StringIO("代碼,錯誤表頭\nA00,0\n"))
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `python -m pytest tests/test_convert.py -v`
Expected: FAIL（`ModuleNotFoundError: convert`）。若 pytest 未安裝：`pip install pytest`

- [ ] **Step 3: 寫 build/convert.py**

```python
"""健保署 CSV → data/codes.min.json（[[code, use, en, zh], ...]）"""
import csv, io, json, sys
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
RAW = ROOT / "data" / "icd10cm_2023_zh.csv"
OUT = ROOT / "data" / "codes.min.json"

def parse_rows(fileobj):
    reader = csv.reader(fileobj)
    header = next(reader)
    # 第一欄名含 BOM 時剝除
    header[0] = header[0].lstrip("﻿")
    assert "ICD-10" in header[0] and header[1].strip().upper() == "USE", f"表頭不符預期: {header}"
    out = []
    for r in reader:
        if not r or not r[0].strip():
            continue
        code = r[0].strip().upper()
        use = 1 if r[1].strip() == "1" else 0
        out.append([code, use, r[2].strip(), r[3].strip()])
    return out

def main():
    sys.stdout.reconfigure(encoding="utf-8")
    with io.open(RAW, encoding="utf-8-sig", newline="") as f:
        rows = parse_rows(f)
    codes = [r[0] for r in rows]
    assert len(codes) == len(set(codes)), "代碼重複"
    with io.open(OUT, "w", encoding="utf-8", newline="\n") as f:
        json.dump(rows, f, ensure_ascii=False, separators=(",", ":"))
    n_leaf = sum(1 for r in rows if r[1] == 1)
    print(f"總筆數 {len(rows)}, 葉碼 {n_leaf}, 類目 {len(rows) - n_leaf}")

if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 跑測試確認通過**

Run: `python -m pytest tests/test_convert.py -v`
Expected: 2 PASS

- [ ] **Step 5: 對真實資料執行**

Run: `python build/convert.py`
Expected: `總筆數 96802, 葉碼 73681, 類目 23121`（與 Global Constraints 的已驗證事實一致，不一致即停手回報）

- [ ] **Step 6: Commit**

```bash
git add build/convert.py tests/test_convert.py
git commit -m "feat: CSV 轉檔器（含表頭驗證與去重）"
```

---

### Task 3: 全庫資料完整性測試

**Files:**
- Create: `tests/test_data_integrity.py`

**Interfaces:**
- Consumes: `data/codes.min.json`（Task 2 產出）。

- [ ] **Step 1: 寫測試（直接對真實產出檔驗證，這是資料品質關卡不是單元測試）**

```python
import io, json, re
from pathlib import Path
import pytest

DATA = Path(__file__).resolve().parent.parent / "data" / "codes.min.json"
# 簡體專用字集（繁簡共用字不可入列，見全域 environment-gotchas）
SIMPLIFIED = set("们这时东买卖说话开关进过还没体医药区实现么头几")

@pytest.fixture(scope="module")
def rows():
    if not DATA.exists():
        pytest.skip("codes.min.json 不存在，先跑 build/convert.py")
    return json.loads(DATA.read_text(encoding="utf-8"))

def test_counts(rows):
    assert len(rows) == 96802
    assert sum(1 for r in rows if r[1] == 1) == 73681

def test_no_replacement_char(rows):
    assert not [r for r in rows if "�" in r[2] or "�" in r[3]]

def test_code_format(rows):
    pat = re.compile(r"^[A-Z][0-9][0-9A-Z](\.[0-9A-Z]{1,4})?$")
    bad = [r[0] for r in rows if not pat.match(r[0])]
    assert not bad, f"格式異常代碼（前10）: {bad[:10]}"

def test_no_simplified_chars(rows):
    hits = [(r[0], ch) for r in rows for ch in SIMPLIFIED if ch in r[3]]
    assert not hits, f"疑似簡體字（前10，若為誤報請將該字自 SIMPLIFIED 移除並註記）: {hits[:10]}"

def test_all_have_names(rows):
    assert not [r for r in rows if not r[2] or not r[3]]
```

- [ ] **Step 2: 執行**

Run: `python -m pytest tests/test_data_integrity.py -v`
Expected: 5 PASS。若 `test_no_simplified_chars` 失敗，逐筆人工判讀：真簡體→回報停手（來源資料問題）；誤報→自字集移除該字並在 commit 訊息註記。

- [ ] **Step 3: Commit**

```bash
git add tests/test_data_integrity.py
git commit -m "test: 全庫資料完整性檢查（筆數/亂碼/簡體/格式）"
```

---

### Task 4: logic.js — 搜尋、家族碼、清單格式化（node --test TDD）

**Files:**
- Create: `src/logic.js`
- Test: `tests/logic.test.mjs`

**Interfaces:**
- Produces（瀏覽器掛 `window.ICDLogic`、node 為 module.exports）：
  - `buildIndex(db)` → `{db, nodot:string[], lowerEn:string[], byCode:Map<code,row>}`；db 為 Task 2 的 `[code,use,en,zh]` 陣列
  - `search(index, query, limit=50)` → row 陣列；代碼前綴優先（忽略小數點、不分大小寫），其後英文子字串（不分大小寫）、中文子字串；去重；query 長度 <2 回空陣列
  - `family(index, code, limit=20)` → 同前三碼類目的**葉碼** row 陣列（排除自身）
  - `formatCart(items, fmt)` → string；items 為 `[{code,zh}]`；fmt：`"lines"`（每行一碼）/`"comma"`（`,` 分隔）/`"names"`（每行 `code\tzh`）

- [ ] **Step 1: 確認 node 可用**

Run: `node --version`
Expected: v18+。若無：`winget install OpenJS.NodeJS.LTS` 後重開 shell。

- [ ] **Step 2: 寫失敗測試 tests/logic.test.mjs**

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { createRequire } from 'node:module';
const require = createRequire(import.meta.url);
const L = require('../src/logic.js');

const DB = [
  ['E11', 0, 'Type 2 diabetes mellitus', '第2型糖尿病'],
  ['E11.9', 1, 'Type 2 diabetes mellitus without complications', '第2型糖尿病伴無併發症'],
  ['E11.65', 1, 'Type 2 diabetes mellitus with hyperglycemia', '第2型糖尿病伴高血糖'],
  ['L03.90', 1, 'Cellulitis, unspecified', '蜂窩性組織炎'],
  ['I10', 1, 'Essential (primary) hypertension', '本態性高血壓'],
];
const idx = L.buildIndex(DB);

test('search: 代碼前綴（忽略小數點與大小寫）優先', () => {
  const r = L.search(idx, 'e119');
  assert.equal(r[0][0], 'E11.9');
});
test('search: 英文子字串', () => {
  assert.equal(L.search(idx, 'cellul')[0][0], 'L03.90');
});
test('search: 中文子字串', () => {
  assert.equal(L.search(idx, '蜂窩')[0][0], 'L03.90');
});
test('search: 過短 query 回空', () => {
  assert.deepEqual(L.search(idx, 'e'), []);
});
test('search: 代碼與名稱混合命中不重複', () => {
  const codes = L.search(idx, 'E11').map(r => r[0]);
  assert.deepEqual(codes, [...new Set(codes)]);
  assert.ok(codes.includes('E11.9') && codes.includes('E11.65'));
});
test('family: 同類目葉碼、排除自身與類目碼', () => {
  const codes = L.family(idx, 'E11.9').map(r => r[0]);
  assert.deepEqual(codes, ['E11.65']);
});
test('formatCart 三種格式', () => {
  const items = [{ code: 'I10', zh: '本態性高血壓' }, { code: 'E11.9', zh: '第2型糖尿病伴無併發症' }];
  assert.equal(L.formatCart(items, 'lines'), 'I10\nE11.9');
  assert.equal(L.formatCart(items, 'comma'), 'I10,E11.9');
  assert.equal(L.formatCart(items, 'names'), 'I10\t本態性高血壓\nE11.9\t第2型糖尿病伴無併發症');
});
```

- [ ] **Step 3: 跑測試確認失敗**

Run: `node --test tests/logic.test.mjs`
Expected: FAIL（Cannot find module '../src/logic.js'）

- [ ] **Step 4: 寫 src/logic.js**

```js
/* 純邏輯層：瀏覽器掛 window.ICDLogic；node 供測試 require。 */
(function (root, factory) {
  if (typeof module === 'object' && module.exports) module.exports = factory();
  else root.ICDLogic = factory();
})(typeof self !== 'undefined' ? self : this, function () {
  'use strict';

  function buildIndex(db) {
    const nodot = new Array(db.length);
    const lowerEn = new Array(db.length);
    const byCode = new Map();
    for (let i = 0; i < db.length; i++) {
      nodot[i] = db[i][0].replace('.', '');
      lowerEn[i] = db[i][2].toLowerCase();
      byCode.set(db[i][0], db[i]);
    }
    return { db, nodot, lowerEn, byCode };
  }

  function search(index, query, limit) {
    limit = limit || 50;
    const q = (query || '').trim();
    if (q.length < 2) return [];
    const out = [];
    const seen = new Set();
    const push = (row) => { if (!seen.has(row[0])) { seen.add(row[0]); out.push(row); } };
    const { db, nodot, lowerEn } = index;
    if (/^[a-z][0-9a-z.]*$/i.test(q)) {           // 像代碼：先做代碼前綴
      const qc = q.toUpperCase().replace(/\./g, '');
      for (let i = 0; i < db.length && out.length < limit; i++)
        if (nodot[i].startsWith(qc)) push(db[i]);
    }
    const ql = q.toLowerCase();
    for (let i = 0; i < db.length && out.length < limit; i++)
      if (lowerEn[i].includes(ql) || db[i][3].includes(q)) push(db[i]);
    return out;
  }

  function family(index, code, limit) {
    limit = limit || 20;
    const cat = code.slice(0, 3);
    const out = [];
    for (const row of index.db) {
      if (row[1] === 1 && row[0] !== code && row[0].slice(0, 3) === cat) {
        out.push(row);
        if (out.length >= limit) break;
      }
    }
    return out;
  }

  function formatCart(items, fmt) {
    if (fmt === 'comma') return items.map(x => x.code).join(',');
    if (fmt === 'names') return items.map(x => x.code + '\t' + x.zh).join('\n');
    return items.map(x => x.code).join('\n');
  }

  return { buildIndex, search, family, formatCart };
});
```

- [ ] **Step 5: 跑測試確認通過**

Run: `node --test tests/logic.test.mjs`
Expected: 7 PASS

- [ ] **Step 6: Commit**

```bash
git add src/logic.js tests/logic.test.mjs
git commit -m "feat: 搜尋/家族碼/清單格式化核心邏輯"
```

---

### Task 5: 精選內容（一）— 慢性病、感染科、病原附加碼 ＋ 驗證測試

**Files:**
- Create: `src/curated/chronic.json`, `src/curated/infectious.json`, `src/curated/pathogens.json`
- Create: `build/lookup.py`
- Test: `tests/test_curated.py`

**Interfaces:**
- Produces: 三個 JSON，格式 `[[code, label], ...]`（label＝門診慣用繁中短名，可含慣用英文縮寫）。
- Produces: `tests/test_curated.py` 自動掃描 `src/curated/` 下**所有** JSON（含之後 Task 6–8 新增者），驗證每個碼存在於全庫且 USE=1 —— 後續 Task 不需再改此測試。
- Produces: `build/lookup.py`：`python build/lookup.py 蜂窩` 或 `python build/lookup.py L03` 印出全庫符合列（開發輔助）。

- [ ] **Step 1: 寫 build/lookup.py（找碼輔助工具）**

```python
"""關鍵字/代碼前綴查全庫。用法：python build/lookup.py <關鍵字> [上限]"""
import io, json, sys
from pathlib import Path

def main():
    sys.stdout.reconfigure(encoding="utf-8")
    q = sys.argv[1]
    limit = int(sys.argv[2]) if len(sys.argv) > 2 else 30
    rows = json.loads((Path(__file__).resolve().parent.parent / "data" / "codes.min.json").read_text(encoding="utf-8"))
    ql, qc = q.lower(), q.upper().replace(".", "")
    n = 0
    for code, use, en, zh in rows:
        if code.replace(".", "").startswith(qc) or ql in en.lower() or q in zh:
            print(f"{code}\tUSE={use}\t{zh}\t{en}")
            n += 1
            if n >= limit: break

if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 寫驗證測試 tests/test_curated.py**

```python
import io, json
from pathlib import Path
import pytest

ROOT = Path(__file__).resolve().parent.parent
CURATED_DIR = ROOT / "src" / "curated"

@pytest.fixture(scope="module")
def leafset():
    rows = json.loads((ROOT / "data" / "codes.min.json").read_text(encoding="utf-8"))
    return {r[0] for r in rows if r[1] == 1}

def iter_codes(obj):
    """從任一 curated JSON 結構抽出所有代碼字串。"""
    if isinstance(obj, dict):                      # related.json: {code: [code,...]}
        for k, v in obj.items():
            yield k
            yield from iter_codes(v)
    elif isinstance(obj, list):
        if obj and isinstance(obj[0], str):        # [code, label] 或 [code, code, ...]
            yield obj[0]
            for x in obj[1:]:
                if isinstance(x, str) and len(x) <= 8 and x[:1].isalpha() and any(c.isdigit() for c in x):
                    yield x                        # related 值陣列的碼；label 不會同時滿足這些條件
        else:
            for x in obj:
                if isinstance(x, dict) and "codes" in x:   # 面板 {name, codes}
                    for c in x["codes"]:
                        yield c[0]
                else:
                    yield from iter_codes(x)

def test_all_curated_codes_are_billable_leaves(leafset):
    files = sorted(CURATED_DIR.glob("*.json"))
    assert files, "src/curated/ 下沒有 JSON"
    bad = []
    for f in files:
        data = json.loads(f.read_text(encoding="utf-8"))
        for code in iter_codes(data):
            if code not in leafset:
                bad.append(f"{f.name}: {code}")
    assert not bad, "非葉碼或不存在（用 build/lookup.py 查正確碼替換）:\n" + "\n".join(bad)

def test_no_duplicate_within_each_quick_list():
    for name in ["chronic.json", "infectious.json", "pathogens.json"]:
        f = CURATED_DIR / name
        if not f.exists():
            continue
        codes = [c for c, _ in json.loads(f.read_text(encoding="utf-8"))]
        assert len(codes) == len(set(codes)), f"{name} 有重複碼"
```

注意 `iter_codes` 對 `[code, label]` 只取第一元素當碼；related.json 的值陣列全是碼。label 誤判為碼的條件（≤8字元＋字母開頭＋含數字）在中文 label 上不會成立。

- [ ] **Step 3: 寫 chronic.json（內科常見慢性病）**

```json
[
  ["I10", "高血壓"],
  ["E11.9", "第2型糖尿病"],
  ["E11.65", "糖尿病伴高血糖"],
  ["E78.5", "高脂血症"],
  ["E78.00", "高膽固醇血症"],
  ["E78.2", "混合型高脂血症"],
  ["E79.0", "高尿酸血症"],
  ["M10.9", "痛風"],
  ["N18.30", "CKD 第3期"],
  ["N18.31", "CKD 3a"],
  ["N18.32", "CKD 3b"],
  ["N18.4", "CKD 第4期"],
  ["N18.5", "CKD 第5期"],
  ["N18.6", "末期腎病 ESRD"],
  ["I25.10", "冠心病 CAD"],
  ["I48.91", "心房顫動 Af"],
  ["I50.9", "心衰竭 HF"],
  ["I69.30", "腦中風後遺症"],
  ["Z86.73", "中風病史(無後遺症)"],
  ["J44.9", "COPD"],
  ["J45.909", "氣喘"],
  ["K21.9", "胃食道逆流 GERD"],
  ["K29.50", "慢性胃炎"],
  ["B18.1", "慢性 B 型肝炎"],
  ["B18.2", "慢性 C 型肝炎"],
  ["K74.60", "肝硬化"],
  ["K76.0", "脂肪肝"],
  ["E03.9", "甲狀腺功能低下"],
  ["E05.90", "甲狀腺功能亢進"],
  ["N40.0", "攝護腺肥大 BPH"],
  ["N40.1", "BPH 伴下泌尿道症狀"],
  ["M81.0", "骨質疏鬆"],
  ["F03.90", "失智症"],
  ["G47.00", "失眠"],
  ["F41.9", "焦慮症"],
  ["F32.A", "憂鬱症"],
  ["D64.9", "貧血"],
  ["D50.9", "缺鐵性貧血"],
  ["E66.9", "肥胖"]
]
```

- [ ] **Step 4: 寫 infectious.json（感染科常見）**

```json
[
  ["J06.9", "急性上呼吸道感染 URI"],
  ["J02.9", "急性咽炎"],
  ["J02.0", "鏈球菌性咽炎"],
  ["J03.90", "急性扁桃腺炎"],
  ["J20.9", "急性支氣管炎"],
  ["J18.9", "肺炎"],
  ["J15.9", "細菌性肺炎"],
  ["J69.0", "吸入性肺炎"],
  ["J11.1", "流感"],
  ["U07.1", "COVID-19"],
  ["U09.9", "COVID 後症候群"],
  ["A15.0", "肺結核"],
  ["A31.0", "肺 NTM 感染"],
  ["N39.0", "泌尿道感染 UTI"],
  ["N30.00", "急性膀胱炎"],
  ["N10", "急性腎盂腎炎 APN"],
  ["N41.0", "急性攝護腺炎"],
  ["L03.90", "蜂窩性組織炎"],
  ["L03.115", "蜂窩性組織炎(右下肢)"],
  ["L03.116", "蜂窩性組織炎(左下肢)"],
  ["A46", "丹毒"],
  ["L02.91", "皮膚膿瘍"],
  ["B02.9", "帶狀疱疹"],
  ["B00.9", "單純疱疹"],
  ["B35.1", "甲癬"],
  ["B35.3", "足癬"],
  ["B37.9", "念珠菌感染"],
  ["B86", "疥瘡"],
  ["A09", "感染性腸胃炎"],
  ["A08.4", "病毒性腸炎"],
  ["A02.0", "沙門氏菌腸炎"],
  ["A04.72", "困難梭菌感染 CDI"],
  ["K75.0", "肝膿瘍"],
  ["K81.0", "急性膽囊炎"],
  ["K83.09", "急性膽管炎"],
  ["K65.2", "自發性細菌性腹膜炎 SBP"],
  ["A41.9", "敗血症"],
  ["R78.81", "菌血症"],
  ["I33.0", "感染性心內膜炎 IE"],
  ["M86.9", "骨髓炎"],
  ["A90", "登革熱"],
  ["A75.3", "恙蟲病"],
  ["B20", "HIV 感染症"],
  ["Z21", "無症狀 HIV 感染"],
  ["A53.9", "梅毒"],
  ["A54.9", "淋病"]
]
```

- [ ] **Step 5: 寫 pathogens.json（病原體附加碼＋抗藥性碼）**

```json
[
  ["B95.0", "A 群鏈球菌 GAS"],
  ["B95.1", "B 群鏈球菌 GBS"],
  ["B95.2", "腸球菌"],
  ["B95.5", "鏈球菌(未明示)"],
  ["B95.61", "MSSA"],
  ["B95.62", "MRSA"],
  ["B96.0", "黴漿菌"],
  ["B96.1", "克雷白氏肺炎桿菌"],
  ["B96.20", "大腸桿菌 E. coli"],
  ["B96.4", "變形桿菌 Proteus"],
  ["B96.5", "綠膿桿菌"],
  ["B96.81", "幽門螺旋桿菌"],
  ["B96.89", "其他細菌"],
  ["B97.4", "呼吸道融合病毒 RSV"],
  ["Z16.12", "ESBL 抗藥性"],
  ["Z16.21", "Vancomycin 抗藥性"],
  ["Z16.24", "多重抗生素抗藥性"]
]
```

- [ ] **Step 6: 跑驗證，逐一修正失敗碼**

Run: `python -m pytest tests/test_curated.py -v`
Expected: 2 PASS。失敗訊息會列出每個問題碼——逐一 `python build/lookup.py <中文或英文關鍵字>` 找正確葉碼替換後重跑，直到全過。**這一步是本計畫防記憶錯碼的機器關卡，不可跳過。**

- [ ] **Step 7: Commit**

```bash
git add src/curated/ build/lookup.py tests/test_curated.py
git commit -m "feat: 慢性病/感染科/病原附加碼精選清單與機器驗證"
```

---

### Task 6: 精選內容（二）— 內科症狀導向面板

**Files:**
- Create: `src/curated/symptoms.json`

**Interfaces:**
- Produces: `[{name:str, codes:[[code,label],...]}, ...]`；Task 9 的面板渲染依賴此格式。
- Consumes: `tests/test_curated.py` 自動涵蓋本檔（無需改測試）。

- [ ] **Step 1: 寫 symptoms.json（23 個面板；症狀碼在前、鑑別診斷在後）**

```json
[
  {"name": "發燒", "codes": [["R50.9","發燒"],["A49.9","細菌感染"],["B34.9","病毒感染"],["J06.9","上呼吸道感染"],["U07.1","COVID-19"],["J11.1","流感"],["A41.9","敗血症"],["R68.83","寒顫"]]},
  {"name": "咳嗽／感冒", "codes": [["R05.9","咳嗽"],["R05.1","急性咳嗽"],["R05.3","慢性咳嗽"],["J00","感冒"],["J06.9","上呼吸道感染"],["J20.9","急性支氣管炎"],["J18.9","肺炎"],["J30.9","過敏性鼻炎"],["J45.901","氣喘急性發作"],["J44.1","COPD 急性惡化"]]},
  {"name": "喉嚨痛", "codes": [["R07.0","喉嚨痛"],["J02.9","急性咽炎"],["J02.0","鏈球菌性咽炎"],["J03.90","急性扁桃腺炎"],["J04.0","急性喉炎"]]},
  {"name": "呼吸困難", "codes": [["R06.02","呼吸急促"],["R06.00","呼吸困難"],["R06.2","喘鳴"],["I50.9","心衰竭"],["J44.1","COPD 急性惡化"],["J45.901","氣喘急性發作"]]},
  {"name": "頭痛", "codes": [["R51.9","頭痛"],["G43.909","偏頭痛"],["G44.209","緊張型頭痛"],["R51.0","姿勢性頭痛"]]},
  {"name": "頭暈／眩暈", "codes": [["R42","頭暈"],["H81.10","良性陣發性眩暈 BPPV"],["H81.20","前庭神經炎"],["I95.1","姿勢性低血壓"],["R55","暈厥"]]},
  {"name": "胸痛／心悸", "codes": [["R07.9","胸痛"],["R07.89","其他胸痛(胸壁)"],["R00.2","心悸"],["I20.9","心絞痛"],["K21.9","胃食道逆流"],["R00.0","心搏過速"]]},
  {"name": "腹痛", "codes": [["R10.9","腹痛"],["R10.13","上腹痛"],["R10.11","右上腹痛"],["R10.31","右下腹痛"],["R10.84","廣泛性腹痛"],["K29.70","胃炎"],["K21.9","胃食道逆流"],["K80.20","膽結石"],["K59.00","便秘"]]},
  {"name": "腹瀉", "codes": [["R19.7","腹瀉"],["A09","感染性腸胃炎"],["K52.9","非感染性腸胃炎"],["A08.4","病毒性腸炎"],["K58.0","腸躁症伴腹瀉"],["A04.72","困難梭菌感染"]]},
  {"name": "噁心嘔吐", "codes": [["R11.0","噁心"],["R11.10","嘔吐"],["R11.2","噁心伴嘔吐"],["E86.0","脫水"],["K21.9","胃食道逆流"]]},
  {"name": "便秘／排便異常", "codes": [["K59.00","便秘"],["K64.9","痔瘡"],["R19.4","排便習慣改變"],["K58.9","腸躁症"]]},
  {"name": "排尿症狀", "codes": [["R30.0","排尿疼痛"],["R35.0","頻尿"],["R35.1","夜尿"],["R33.9","尿滯留"],["R32","尿失禁"],["N39.0","泌尿道感染"],["N30.00","急性膀胱炎"],["N40.1","攝護腺肥大伴症狀"]]},
  {"name": "血尿", "codes": [["R31.9","血尿"],["R31.0","肉眼血尿"],["N20.0","腎結石"],["N30.00","急性膀胱炎"],["N39.0","泌尿道感染"]]},
  {"name": "皮疹／搔癢", "codes": [["R21","皮疹"],["L29.9","搔癢"],["L50.9","蕁麻疹"],["L30.9","皮膚炎"],["L23.9","過敏性接觸性皮膚炎"],["B02.9","帶狀疱疹"],["L03.90","蜂窩性組織炎"]]},
  {"name": "關節痛", "codes": [["M25.50","關節痛"],["M25.561","右膝痛"],["M25.562","左膝痛"],["M25.511","右肩痛"],["M25.512","左肩痛"],["M10.9","痛風"],["M06.9","類風濕性關節炎"],["M19.90","骨關節炎"]]},
  {"name": "背痛／頸痛", "codes": [["M54.50","下背痛"],["M54.2","頸痛"],["M54.16","腰椎神經根病變"],["M51.26","腰椎椎間盤突出"],["M47.816","腰椎退化性關節炎"],["M54.30","坐骨神經痛"]]},
  {"name": "肢體麻木", "codes": [["R20.2","感覺異常(麻木)"],["G62.9","多發性神經病變"],["G56.00","腕隧道症候群"]]},
  {"name": "水腫", "codes": [["R60.0","局部水腫"],["R60.9","水腫"],["I50.9","心衰竭"],["I87.2","靜脈功能不全"],["N18.9","慢性腎臟病"]]},
  {"name": "疲倦／體重減輕", "codes": [["R53.83","疲倦"],["R53.1","虛弱"],["R63.4","體重減輕"],["R63.0","食慾不振"],["R61","盜汗"],["D64.9","貧血"],["E03.9","甲狀腺功能低下"]]},
  {"name": "失眠／情緒", "codes": [["G47.00","失眠"],["F41.9","焦慮症"],["F32.A","憂鬱症"]]},
  {"name": "淋巴結腫大", "codes": [["R59.0","局部淋巴結腫大"],["R59.1","全身淋巴結腫大"],["R59.9","淋巴結腫大"]]},
  {"name": "檢驗異常", "codes": [["R73.03","糖尿病前期"],["R73.9","高血糖"],["R74.01","肝指數上升"],["E79.0","高尿酸血症"],["R80.9","蛋白尿"],["R94.6","甲狀腺功能檢查異常"],["D64.9","貧血"],["R79.89","其他血液生化異常"]]},
  {"name": "眼／耳", "codes": [["H10.9","結膜炎"],["B30.9","病毒性結膜炎"],["H66.90","中耳炎"],["H60.90","外耳炎"],["H92.09","耳痛"],["H61.20","耳垢栓塞"]]}
]
```

- [ ] **Step 2: 跑驗證並修正**

Run: `python -m pytest tests/test_curated.py -v`
Expected: PASS（失敗碼用 `build/lookup.py` 修正，同 Task 5 Step 6 流程）

- [ ] **Step 3: Commit**

```bash
git add src/curated/symptoms.json
git commit -m "feat: 內科症狀導向面板（23 面板）"
```

---

### Task 7: 精選內容（三）— 外科面板與快選

**Files:**
- Create: `src/curated/surgical_panels.json`, `src/curated/surgical_quick.json`

**Interfaces:**
- Produces: `surgical_panels.json` 格式同 symptoms.json；`surgical_quick.json` 格式同 chronic.json。

- [ ] **Step 1: 寫 surgical_panels.json（8 面板；S/T 碼一律 initial encounter「A」）**

```json
[
  {"name": "撕裂傷", "codes": [["S01.01XA","頭皮撕裂傷"],["S01.81XA","頭部其他部位撕裂傷"],["S01.511A","嘴唇撕裂傷"],["S61.419A","手部撕裂傷"],["S51.819A","前臂撕裂傷"],["S81.819A","小腿撕裂傷"],["S91.319A","足部撕裂傷"]]},
  {"name": "挫傷／擦傷", "codes": [["S00.03XA","頭皮挫傷"],["S60.229A","手部挫傷"],["S80.11XA","右小腿挫傷"],["S80.12XA","左小腿挫傷"],["S60.519A","手部擦傷"],["T14.90XA","未明示損傷"]]},
  {"name": "傷口處置／術後", "codes": [["Z48.01","術後換藥"],["Z48.02","拆線"],["Z09","治療後追蹤"],["T81.41XA","淺層手術部位感染 SSI"],["T81.31XA","手術傷口裂開"],["Z47.2","移除內固定物"],["Z47.89","骨科術後照護"]]},
  {"name": "燒燙傷", "codes": [["T30.0","燙傷(未明示部位)"],["T20.20XA","頭臉頸二度燙傷"],["T23.209A","手部二度燙傷"],["T24.209A","下肢二度燙傷"]]},
  {"name": "膿瘍／皮膚病灶", "codes": [["L02.91","皮膚膿瘍"],["L05.01","藏毛囊腫伴膿瘍"],["L03.011","甲溝炎(右手指)"],["L03.012","甲溝炎(左手指)"],["L03.031","甲溝炎(右腳趾)"],["L03.032","甲溝炎(左腳趾)"],["L60.0","嵌甲"],["L72.3","皮脂腺囊腫"],["L72.0","表皮囊腫"],["D17.9","脂肪瘤"],["D22.9","色素痣"],["D23.9","皮膚良性腫瘤"]]},
  {"name": "肛門疾患", "codes": [["K64.9","痔瘡"],["K64.5","血栓性外痔"],["K60.2","肛裂"],["K61.0","肛門膿瘍"],["K60.3","肛門瘻管"],["K62.5","肛門直腸出血"]]},
  {"name": "疝氣／腹部", "codes": [["K40.90","腹股溝疝氣"],["K42.9","臍疝氣"],["K43.9","腹壁疝氣"],["K35.80","急性闌尾炎"],["K80.20","膽結石"],["K81.0","急性膽囊炎"],["K56.609","腸阻塞"]]},
  {"name": "扭傷／拉傷", "codes": [["S93.401A","右踝扭傷"],["S93.402A","左踝扭傷"],["S63.509A","腕扭傷"],["S16.1XXA","頸部拉傷"],["M62.830","背部肌肉痙攣"],["Z47.89","骨科術後照護"],["Z47.2","移除內固定物"]]}
]
```

- [ ] **Step 2: 寫 surgical_quick.json**

```json
[
  ["Z48.01", "術後換藥"],
  ["Z48.02", "拆線"],
  ["Z09", "治療後追蹤"],
  ["T81.41XA", "傷口感染 SSI"],
  ["T14.90XA", "外傷(未明示)"],
  ["S61.419A", "手部撕裂傷"],
  ["L02.91", "皮膚膿瘍"],
  ["L03.011", "甲溝炎(右手指)"],
  ["L60.0", "嵌甲"],
  ["L72.3", "皮脂腺囊腫"],
  ["D17.9", "脂肪瘤"],
  ["K64.9", "痔瘡"],
  ["K60.2", "肛裂"],
  ["K40.90", "腹股溝疝氣"],
  ["K35.80", "急性闌尾炎"]
]
```

- [ ] **Step 3: 跑驗證並修正**

Run: `python -m pytest tests/test_curated.py -v`
Expected: PASS。S/T 碼的七碼結構（X 佔位）最容易錯——失敗時用 `python build/lookup.py S80.1` 這類前綴查法找正確完整碼。

- [ ] **Step 4: Commit**

```bash
git add src/curated/surgical_panels.json src/curated/surgical_quick.json
git commit -m "feat: 外科模式面板與快選清單"
```

---

### Task 8: 精選內容（四）— 相關碼關聯表

**Files:**
- Create: `src/curated/related.json`

**Interfaces:**
- Produces: `{code: [code, ...]}`——key 是「剛加入清單的碼」，value 是建議一併考慮的碼（顯示名稱由全庫查出，不在此重複）。

- [ ] **Step 1: 寫 related.json**

```json
{
  "E11.9": ["E11.65","E78.5","I10","E11.22","E11.40","E11.319","E11.51","Z79.4","Z79.84","E66.9"],
  "E11.65": ["E11.9","E78.5","I10","Z79.4","Z79.84"],
  "E11.22": ["N18.30","N18.4","I12.9","D63.1"],
  "I10": ["E78.5","E11.9","I25.10","I11.9","I12.9","I50.9"],
  "E78.5": ["E78.00","E78.2","I10","E11.9","E79.0"],
  "E79.0": ["M10.9","E78.5"],
  "M10.9": ["E79.0","N20.0","I10","M25.561","M25.562"],
  "N18.30": ["I12.9","D63.1","E87.5","N25.81","E11.22"],
  "N18.4": ["I12.9","D63.1","E87.5","N25.81","E11.22"],
  "N18.5": ["I12.9","D63.1","E87.5","N25.81","Z99.2"],
  "N18.6": ["Z99.2","D63.1","E87.5","N25.81","I12.9"],
  "I25.10": ["I10","E78.5","I20.9","Z95.5","Z95.1"],
  "I48.91": ["Z79.01","I50.9","I10","Z86.73"],
  "I50.9": ["I10","I25.10","I48.91","R60.9","N18.9"],
  "I69.30": ["Z86.73","I10","E78.5","I48.91"],
  "J44.9": ["J44.1","J44.0","F17.210","Z99.81","J45.909"],
  "J44.1": ["J44.9","J18.9","B95.3","B96.1"],
  "J45.909": ["J45.901","J30.9","R05.9"],
  "K21.9": ["K29.70","B96.81","R12","K25.9","K44.9"],
  "K29.70": ["B96.81","K21.9","K25.9","R10.13"],
  "K29.50": ["B96.81","K21.9","K25.9","R10.13"],
  "B18.1": ["K74.60","R74.01","B18.2"],
  "B18.2": ["K74.60","R74.01","B18.1"],
  "K74.60": ["B18.1","B18.2","K76.0","R18.8","K65.2","I85.00"],
  "K76.0": ["R74.01","E78.5","E11.9","E66.9"],
  "E03.9": ["E06.3","R94.6"],
  "E05.90": ["E05.00","R94.6","R00.2","I48.91"],
  "N39.0": ["N30.00","N10","B96.20","B96.1","B95.2","Z16.12","R30.0","R35.0","N40.1"],
  "N30.00": ["N39.0","B96.20","R30.0","R31.9"],
  "N10": ["N39.0","B96.20","B96.1","Z16.12","N20.0","R50.9"],
  "N40.0": ["N40.1","R35.1","R33.9"],
  "N40.1": ["R35.0","R35.1","R33.9","N39.0"],
  "N20.0": ["R31.9","N13.30","N23"],
  "J18.9": ["J15.9","J13","J15.212","J69.0","B96.0","B95.3","U07.1","R05.9","R50.9"],
  "J69.0": ["J18.9","R13.10","I69.391"],
  "U07.1": ["J12.82","U09.9","R05.9","R50.9"],
  "L03.90": ["L03.115","L03.116","B95.62","B95.61","B95.0","A46","E11.9","Z16.24"],
  "L03.115": ["B95.62","B95.61","B95.0","E11.9","I87.2","B35.3"],
  "L03.116": ["B95.62","B95.61","B95.0","E11.9","I87.2","B35.3"],
  "A46": ["B95.0","L03.90"],
  "B02.9": ["B02.29"],
  "A09": ["A08.4","K52.9","E86.0","R19.7","R11.2","A02.0","A04.72"],
  "A04.72": ["A04.71","R19.7"],
  "A41.9": ["R78.81","R65.20","N39.0","J18.9","L03.90","B96.20","B95.62"],
  "R78.81": ["A41.9","I33.0","B95.62","B96.20"],
  "I33.0": ["B95.61","B95.62","B95.2","R78.81"],
  "K81.0": ["K80.20","K80.00","K83.09","R10.11"],
  "K80.20": ["K81.0","K83.09","R10.11","K80.50"],
  "K75.0": ["B96.1","K83.09","A06.4"],
  "K65.2": ["K74.60","R18.8"],
  "A15.0": ["R05.3","R04.2"],
  "B20": ["Z21","B37.9","A15.0","B02.9","Z79.899"],
  "A90": ["R50.9","D69.6","R21"],
  "A75.3": ["R50.9"],
  "G47.00": ["F41.9","F32.A"],
  "F32.A": ["F41.9","G47.00","R53.83"],
  "F41.9": ["F32.A","G47.00","R00.2"],
  "D64.9": ["D50.9","D63.1","R53.83"],
  "D50.9": ["D64.9","K92.2","N92.0"],
  "M54.50": ["M54.16","M51.26","M47.816","M54.30","M62.830"],
  "R42": ["H81.10","H81.20","I95.1","R55"],
  "R50.9": ["A49.9","B34.9","J06.9","N39.0","U07.1","J11.1","A41.9"],
  "K59.00": ["K64.9","R19.4","K58.9"],
  "K64.9": ["K64.5","K60.2","K62.5"],
  "K40.90": ["K42.9","K43.9"],
  "T81.41XA": ["B95.61","B95.62","B96.20","Z48.01","Z48.02"],
  "K35.80": ["R10.31","R11.2"]
}
```

- [ ] **Step 2: 跑驗證並修正**

Run: `python -m pytest tests/test_curated.py -v`
Expected: PASS（本檔 key 與 value 全部都會被驗證）

- [ ] **Step 3: Commit**

```bash
git add src/curated/related.json
git commit -m "feat: 相關碼關聯表（慢病組合與感染科組合）"
```

---

### Task 9: template.html 與 app.js

**Files:**
- Create: `src/template.html`, `src/app.js`

**Interfaces:**
- Consumes: `window.ICDLogic`（Task 4）、`window.CURATED`（build.py 由 curated JSON 組出，鍵名：`chronic, infectious, pathogens, symptoms, surgicalPanels, surgicalQuick, related`）、`#icd-data` script 標籤內的 gzip+base64 資料。
- Produces: DOM 約定（E2E 依賴）：`body[data-ready="1"]` 載入完成；`#status` 顯示「已載入 96,802 筆」；`#search`；`#mode-im` / `#mode-surg`；`.panel`（面板）；`.chip[data-code]`（所有可點碼）；`#cart li[data-code]`；`#copy-lines` / `#copy-comma` / `#copy-names`；`#clear-cart`；`#related .chip`；`#toast`。
- template.html 內含兩個佔位字串：`<!--%SCRIPTS%-->`（build 注入所有 JS）與 `%DATA%`（build 注入 base64 資料）。

- [ ] **Step 1: 寫 src/template.html**

```html
<!doctype html>
<html lang="zh-Hant-TW">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>ICD-10 門診導引</title>
<style>
  :root { --accent:#0b6e4f; --bg:#f7f8f9; --card:#fff; --line:#dcdfe3; --text:#1c1e21; --dim:#65707c; }
  * { box-sizing:border-box; }
  body { margin:0; font-family:"Microsoft JhengHei",system-ui,sans-serif; background:var(--bg); color:var(--text); }
  header { display:flex; gap:10px; align-items:center; padding:10px 14px; background:var(--card); border-bottom:1px solid var(--line); position:sticky; top:0; z-index:5; flex-wrap:wrap; }
  header h1 { font-size:16px; margin:0 8px 0 0; color:var(--accent); white-space:nowrap; }
  #search { flex:1; min-width:220px; font-size:16px; padding:8px 12px; border:1px solid var(--line); border-radius:8px; }
  .mode-btn { font-size:15px; padding:8px 16px; border:1px solid var(--line); background:var(--card); border-radius:8px; cursor:pointer; }
  .mode-btn.active { background:var(--accent); color:#fff; border-color:var(--accent); }
  #status { font-size:12px; color:var(--dim); white-space:nowrap; }
  main { display:grid; grid-template-columns:minmax(0,1fr) 340px; gap:12px; padding:12px 14px; max-width:1280px; margin:0 auto; }
  @media (max-width:900px){ main { grid-template-columns:1fr; } #cart-pane { order:-1; } }
  .card { background:var(--card); border:1px solid var(--line); border-radius:10px; padding:10px 12px; margin-bottom:12px; }
  .card h2 { font-size:14px; margin:2px 0 8px; color:var(--dim); }
  .chip { display:inline-flex; align-items:baseline; gap:6px; margin:3px; padding:6px 10px; border:1px solid var(--line); border-radius:16px; background:var(--card); cursor:pointer; font-size:14px; max-width:100%; }
  .chip:hover { border-color:var(--accent); background:#eef7f3; }
  .chip b { color:var(--accent); font-family:Consolas,monospace; white-space:nowrap; }
  .chip span { overflow:hidden; text-overflow:ellipsis; white-space:nowrap; max-width:30em; }
  .chip.cat { opacity:.45; cursor:not-allowed; }
  .panel { border-top:1px solid var(--line); }
  .panel > button { width:100%; text-align:left; font-size:15px; padding:9px 4px; border:0; background:none; cursor:pointer; font-weight:600; }
  .panel > button::before { content:"▸ "; color:var(--accent); }
  .panel.open > button::before { content:"▾ "; }
  .panel > div { display:none; padding-bottom:8px; }
  .panel.open > div { display:block; }
  #cart-pane { position:sticky; top:64px; align-self:start; }
  #cart { list-style:none; margin:0 0 10px; padding:0; min-height:40px; }
  #cart li { display:flex; align-items:center; gap:8px; padding:6px 4px; border-bottom:1px dashed var(--line); font-size:14px; }
  #cart li b { font-family:Consolas,monospace; color:var(--accent); white-space:nowrap; }
  #cart li span { flex:1; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; }
  #cart li button { border:0; background:none; color:#c0392b; cursor:pointer; font-size:14px; }
  #copy-bar { display:flex; gap:6px; flex-wrap:wrap; margin-bottom:6px; }
  #copy-bar button, #clear-cart { font-size:13px; padding:7px 10px; border:1px solid var(--line); border-radius:8px; background:var(--card); cursor:pointer; }
  #copy-bar button:hover { border-color:var(--accent); }
  #copy-lines { background:var(--accent); color:#fff; border-color:var(--accent); }
  #related-wrap h2 { margin-top:4px; }
  #related .group-label { font-size:12px; color:var(--dim); margin:6px 2px 2px; }
  #toast { position:fixed; bottom:24px; left:50%; transform:translateX(-50%); background:#222; color:#fff; padding:9px 18px; border-radius:20px; font-size:14px; opacity:0; transition:opacity .25s; pointer-events:none; z-index:9; }
  #toast.show { opacity:.94; }
  #fallback-copy { position:fixed; inset:0; background:rgba(0,0,0,.4); display:none; align-items:center; justify-content:center; z-index:8; }
  #fallback-copy textarea { width:min(480px,90vw); height:160px; font-size:14px; }
  .hidden { display:none !important; }
</style>
</head>
<body>
<header>
  <h1>ICD-10 門診導引</h1>
  <input id="search" type="search" placeholder="搜尋全庫：中文／英文／代碼（如 蜂窩、cellulitis、L03）" autocomplete="off">
  <button id="mode-im" class="mode-btn active">內科</button>
  <button id="mode-surg" class="mode-btn">外科</button>
  <span id="status">載入資料中…</span>
</header>
<main>
  <section id="left">
    <div class="card hidden" id="results-card"><h2>搜尋結果（僅可申報碼可加入）</h2><div id="search-results"></div></div>
    <div class="card" id="panels-card"><h2 id="panels-title">症狀導向</h2><div id="panels"></div></div>
    <div class="card" id="quick-card"><div id="quick"></div></div>
  </section>
  <aside id="cart-pane">
    <div class="card">
      <h2>本次就診清單 <button id="clear-cart" style="float:right">清空</button></h2>
      <ul id="cart"></ul>
      <div id="copy-bar">
        <button id="copy-lines">複製（每行一碼）</button>
        <button id="copy-comma">逗號分隔</button>
        <button id="copy-names">碼＋名稱</button>
      </div>
    </div>
    <div class="card" id="related-wrap">
      <h2>相關代碼</h2>
      <div id="related"><span style="color:var(--dim);font-size:13px">加入代碼後，這裡會列出建議一併評估的診斷碼。</span></div>
    </div>
  </aside>
</main>
<div id="toast"></div>
<div id="fallback-copy"><div class="card"><p>自動複製失敗，請手動全選複製：</p><textarea readonly></textarea></div></div>
<script id="icd-data" type="application/gzip-base64">%DATA%</script>
<!--%SCRIPTS%-->
</body>
</html>
```

- [ ] **Step 2: 寫 src/app.js**

```js
/* UI 層：依賴 window.ICDLogic 與 window.CURATED（build.py 注入）。 */
(function () {
  'use strict';
  const $ = (sel) => document.querySelector(sel);
  let index = null;
  const cart = [];            // [{code, zh}]
  let mode = 'im';            // 'im' | 'surg'

  // ---- 資料載入 ----
  async function loadCodes() {
    const b64 = $('#icd-data').textContent.trim();
    const bytes = Uint8Array.from(atob(b64), (c) => c.charCodeAt(0));
    const stream = new Blob([bytes]).stream().pipeThrough(new DecompressionStream('gzip'));
    return JSON.parse(await new Response(stream).text());
  }

  // ---- 渲染 ----
  function chip(code, label, cls) {
    const row = index.byCode.get(code);
    const el = document.createElement('button');
    el.type = 'button';
    el.className = 'chip' + (cls ? ' ' + cls : '');
    el.dataset.code = code;
    const zh = row ? row[3] : '';
    el.innerHTML = '<b></b><span></span>';
    el.querySelector('b').textContent = code;
    el.querySelector('span').textContent = label || zh;
    el.title = row ? code + ' ' + row[3] + '\n' + row[2] : code;
    return el;
  }

  function renderPanels() {
    const panels = mode === 'im' ? window.CURATED.symptoms : window.CURATED.surgicalPanels;
    $('#panels-title').textContent = mode === 'im' ? '症狀導向（內科）' : '常見情境（外科）';
    const host = $('#panels');
    host.textContent = '';
    for (const p of panels) {
      const div = document.createElement('div');
      div.className = 'panel';
      const btn = document.createElement('button');
      btn.type = 'button';
      btn.textContent = p.name;
      btn.addEventListener('click', () => div.classList.toggle('open'));
      const body = document.createElement('div');
      for (const [code, label] of p.codes) body.appendChild(chip(code, label));
      div.append(btn, body);
      host.appendChild(div);
    }
  }

  function renderQuick() {
    const host = $('#quick');
    host.textContent = '';
    const groups = mode === 'im'
      ? [['常用慢性病', window.CURATED.chronic], ['感染科常用', window.CURATED.infectious], ['病原體附加碼／抗藥性', window.CURATED.pathogens]]
      : [['外科常用', window.CURATED.surgicalQuick], ['病原體附加碼／抗藥性', window.CURATED.pathogens]];
    for (const [title, list] of groups) {
      const h = document.createElement('h2');
      h.textContent = title;
      host.appendChild(h);
      for (const [code, label] of list) host.appendChild(chip(code, label));
    }
  }

  function renderCart() {
    const ul = $('#cart');
    ul.textContent = '';
    for (const item of cart) {
      const li = document.createElement('li');
      li.dataset.code = item.code;
      li.innerHTML = '<b></b><span></span><button title="移除">✕</button>';
      li.querySelector('b').textContent = item.code;
      li.querySelector('span').textContent = item.zh;
      li.querySelector('button').addEventListener('click', () => {
        cart.splice(cart.findIndex((x) => x.code === item.code), 1);
        renderCart();
      });
      ul.appendChild(li);
    }
  }

  function renderRelated(code) {
    const host = $('#related');
    host.textContent = '';
    const curated = (window.CURATED.related[code] || []).filter((c) => !cart.some((x) => x.code === c));
    const fam = window.ICDLogic.family(index, code, 20).filter((r) => !cart.some((x) => x.code === r[0]));
    if (curated.length) {
      const lb = document.createElement('div');
      lb.className = 'group-label';
      lb.textContent = '臨床常一併評估（' + code + '）';
      host.appendChild(lb);
      for (const c of curated) host.appendChild(chip(c, ''));
    }
    if (fam.length) {
      const lb = document.createElement('div');
      lb.className = 'group-label';
      lb.textContent = '同類目其他碼（' + code.slice(0, 3) + '）';
      host.appendChild(lb);
      for (const r of fam) host.appendChild(chip(r[0], ''));
    }
    if (!curated.length && !fam.length) host.innerHTML = '<span style="color:var(--dim);font-size:13px">無相關碼建議。</span>';
  }

  function renderResults(rows) {
    const card = $('#results-card');
    const host = $('#search-results');
    host.textContent = '';
    if (!rows) { card.classList.add('hidden'); return; }
    card.classList.remove('hidden');
    if (!rows.length) { host.innerHTML = '<span style="color:var(--dim)">查無結果，試試英文或代碼前綴。</span>'; return; }
    for (const r of rows) host.appendChild(chip(r[0], '', r[1] === 1 ? '' : 'cat'));
  }

  // ---- 動作 ----
  function toast(msg) {
    const t = $('#toast');
    t.textContent = msg;
    t.classList.add('show');
    clearTimeout(toast._h);
    toast._h = setTimeout(() => t.classList.remove('show'), 1600);
  }

  function addCode(code) {
    const row = index.byCode.get(code);
    if (!row || row[1] !== 1) return;
    if (cart.some((x) => x.code === code)) { toast(code + ' 已在清單'); return; }
    cart.push({ code, zh: row[3] });
    renderCart();
    renderRelated(code);
    toast('已加入 ' + code);
  }

  async function copyText(text) {
    try {
      await navigator.clipboard.writeText(text);
      return true;
    } catch (e) {
      try {
        const ta = document.createElement('textarea');
        ta.value = text;
        document.body.appendChild(ta);
        ta.select();
        const ok = document.execCommand('copy');
        ta.remove();
        if (ok) return true;
      } catch (e2) { /* fall through */ }
      const box = $('#fallback-copy');
      box.style.display = 'flex';
      box.querySelector('textarea').value = text;
      box.addEventListener('click', () => (box.style.display = 'none'), { once: true });
      return false;
    }
  }

  async function copyCart(fmt) {
    if (!cart.length) { toast('清單是空的'); return; }
    if (await copyText(window.ICDLogic.formatCart(cart, fmt))) toast('已複製 ' + cart.length + ' 個代碼');
  }

  function setMode(m) {
    mode = m;
    $('#mode-im').classList.toggle('active', m === 'im');
    $('#mode-surg').classList.toggle('active', m === 'surg');
    renderPanels();
    renderQuick();
  }

  // ---- 事件 ----
  function wire() {
    document.body.addEventListener('click', (ev) => {
      const c = ev.target.closest('.chip');
      if (c && !c.classList.contains('cat')) addCode(c.dataset.code);
    });
    let debounce = null;
    $('#search').addEventListener('input', (ev) => {
      clearTimeout(debounce);
      const q = ev.target.value;
      debounce = setTimeout(() => {
        renderResults(q.trim().length >= 2 ? window.ICDLogic.search(index, q, 50) : null);
      }, 150);
    });
    $('#mode-im').addEventListener('click', () => setMode('im'));
    $('#mode-surg').addEventListener('click', () => setMode('surg'));
    $('#copy-lines').addEventListener('click', () => copyCart('lines'));
    $('#copy-comma').addEventListener('click', () => copyCart('comma'));
    $('#copy-names').addEventListener('click', () => copyCart('names'));
    $('#clear-cart').addEventListener('click', () => { cart.length = 0; renderCart(); });
  }

  // ---- 啟動 ----
  async function init() {
    if (typeof DecompressionStream === 'undefined') {
      $('#status').textContent = '瀏覽器過舊：請使用 Edge 或 Chrome 80 以上版本。';
      return;
    }
    try {
      const db = await loadCodes();
      index = window.ICDLogic.buildIndex(db);
      renderPanels();
      renderQuick();
      wire();
      $('#status').textContent = '已載入 ' + db.length.toLocaleString() + ' 筆';
      document.body.dataset.ready = '1';
    } catch (e) {
      $('#status').textContent = '資料載入失敗：' + e.message;
    }
  }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', init);
  else init();
})();
```

- [ ] **Step 3: 語法煙霧測試（node 只檢查可解析，不執行 DOM）**

Run: `node --check src/app.js && node --check src/logic.js`
Expected: 無輸出（exit 0）

- [ ] **Step 4: Commit**

```bash
git add src/template.html src/app.js
git commit -m "feat: 單頁 UI（面板/快選/清單/相關碼/複製）"
```

---

### Task 10: build.py — 組裝單一 HTML

**Files:**
- Create: `build/build.py`
- Test: `tests/test_build.py`

**Interfaces:**
- Consumes: `src/template.html`（`%DATA%`、`<!--%SCRIPTS%-->` 佔位）、`src/logic.js`、`src/app.js`、`src/curated/*.json`、`data/codes.min.json`。
- Produces: `dist/icd10.html`；CURATED 鍵名映射固定為：`chronic.json→chronic, infectious.json→infectious, pathogens.json→pathogens, symptoms.json→symptoms, surgical_panels.json→surgicalPanels, surgical_quick.json→surgicalQuick, related.json→related`。

- [ ] **Step 1: 寫失敗測試 tests/test_build.py**

```python
import subprocess, sys
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
DIST = ROOT / "dist" / "icd10.html"

def test_build_produces_single_html():
    r = subprocess.run([sys.executable, str(ROOT / "build" / "build.py")], capture_output=True, text=True)
    assert r.returncode == 0, r.stderr
    html = DIST.read_text(encoding="utf-8")
    assert "%DATA%" not in html and "%SCRIPTS%" not in html, "佔位符未被取代"
    assert "window.CURATED" in html and "ICDLogic" in html
    assert 'type="application/gzip-base64"' in html
    assert "http://" not in html and "https://" not in html.replace("https://info.nhi.gov.tw", ""), "不得有外部資源參照"
    size = DIST.stat().st_size
    assert 1_000_000 < size <= 6_000_000, f"檔案大小異常: {size:,} bytes"
```

（`https://info.nhi.gov.tw` 例外是允許註解中標示資料來源。）

- [ ] **Step 2: 跑測試確認失敗**

Run: `python -m pytest tests/test_build.py -v`
Expected: FAIL（build.py 不存在）

- [ ] **Step 3: 寫 build/build.py**

```python
"""組裝單一離線 HTML：資料 gzip+base64 內嵌、全部 JS inline。"""
import base64, gzip, io, json, sys
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
SRC, DATA, DIST = ROOT / "src", ROOT / "data", ROOT / "dist"
CURATED_KEYS = {
    "chronic.json": "chronic", "infectious.json": "infectious", "pathogens.json": "pathogens",
    "symptoms.json": "symptoms", "surgical_panels.json": "surgicalPanels",
    "surgical_quick.json": "surgicalQuick", "related.json": "related",
}

def main():
    sys.stdout.reconfigure(encoding="utf-8")
    raw = (DATA / "codes.min.json").read_bytes()
    b64 = base64.b64encode(gzip.compress(raw, 9)).decode("ascii")

    curated = {}
    for fname, key in CURATED_KEYS.items():
        curated[key] = json.loads((SRC / "curated" / fname).read_text(encoding="utf-8"))
    scripts = (
        "<script>\n/* 資料來源：健保署 2023年版中文 ICD-10-CM（政府資料開放平臺 dataset 177507） */\n"
        + "window.CURATED = " + json.dumps(curated, ensure_ascii=False, separators=(",", ":")) + ";\n</script>\n"
        + "<script>\n" + (SRC / "logic.js").read_text(encoding="utf-8") + "\n</script>\n"
        + "<script>\n" + (SRC / "app.js").read_text(encoding="utf-8") + "\n</script>"
    )

    html = (SRC / "template.html").read_text(encoding="utf-8")
    html = html.replace("%DATA%", b64).replace("<!--%SCRIPTS%-->", scripts)
    DIST.mkdir(exist_ok=True)
    out = DIST / "icd10.html"
    with io.open(out, "w", encoding="utf-8", newline="\n") as f:
        f.write(html)
    print(f"輸出 {out}（{out.stat().st_size:,} bytes）")

if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 跑測試確認通過**

Run: `python -m pytest tests/test_build.py -v`
Expected: PASS，並注意印出的實際檔案大小

- [ ] **Step 5: 手動煙霧測試（瀏覽器實開）**

用瀏覽器開啟 `dist/icd10.html`（file://），確認：狀態列顯示「已載入 96,802 筆」、面板可展開、點碼會進清單。有任何 console 錯誤即修復後重跑 Step 4。

- [ ] **Step 6: Commit（含 dist 交付物）**

```bash
git add build/build.py tests/test_build.py dist/icd10.html
git commit -m "feat: 建置管線與首版單檔輸出"
```

---

### Task 11: Playwright E2E

**Files:**
- Create: `tests/e2e_test.py`

**Interfaces:**
- Consumes: Task 9 的 DOM 約定與 `dist/icd10.html`。
- 剪貼簿需要 secure context，E2E 以 `http://localhost` 提供 dist（`http.server`），瀏覽器授予 clipboard 權限。

- [ ] **Step 1: 確認 Playwright 可用**

Run: `python -c "import playwright" || pip install playwright && python -m playwright install chromium`
Expected: 無錯誤

- [ ] **Step 2: 寫 tests/e2e_test.py**

```python
"""E2E：對 dist/icd10.html 實跑主要使用流程。先跑 build/build.py。"""
import http.server, json, threading
from functools import partial
from pathlib import Path
import pytest
from playwright.sync_api import sync_playwright, expect

ROOT = Path(__file__).resolve().parent.parent
PORT = 18493

@pytest.fixture(scope="module")
def page_url():
    handler = partial(http.server.SimpleHTTPRequestHandler, directory=str(ROOT / "dist"))
    srv = http.server.ThreadingHTTPServer(("127.0.0.1", PORT), handler)
    t = threading.Thread(target=srv.serve_forever, daemon=True)
    t.start()
    yield f"http://127.0.0.1:{PORT}/icd10.html"
    srv.shutdown()

@pytest.fixture(scope="module")
def page(page_url):
    with sync_playwright() as p:
        browser = p.chromium.launch()
        ctx = browser.new_context(permissions=["clipboard-read", "clipboard-write"])
        pg = ctx.new_page()
        pg.goto(page_url)
        pg.wait_for_selector('body[data-ready="1"]', timeout=5000)
        yield pg
        browser.close()

def reset(pg):
    pg.click("#clear-cart")
    pg.fill("#search", "")

def test_load_and_status(page):
    expect(page.locator("#status")).to_contain_text("96,802")

def test_load_performance(page_url, page):
    ms = page.evaluate("""() => {
        const [nav] = performance.getEntriesByType('navigation');
        return nav.domContentLoadedEventEnd - nav.startTime;
    }""")
    assert ms < 2000, f"載入至 DOMContentLoaded 花了 {ms:.0f}ms"

def test_search_english_chinese_code(page):
    reset(page)
    page.fill("#search", "cellulitis")
    expect(page.locator("#search-results .chip").first).to_contain_text("L03", timeout=2000)
    page.fill("#search", "蜂窩")
    expect(page.locator("#search-results .chip").first).to_contain_text("蜂窩", timeout=2000)
    page.fill("#search", "E119")
    expect(page.locator("#search-results .chip").first).to_contain_text("E11.9", timeout=2000)

def test_category_code_not_addable(page):
    reset(page)
    page.fill("#search", "E11")
    page.wait_for_selector("#search-results .chip.cat")
    page.locator("#search-results .chip.cat").first.click()
    assert page.locator("#cart li").count() == 0

def test_quick_add_and_related(page):
    reset(page)
    page.locator('.chip[data-code="N39.0"]').first.click()
    expect(page.locator('#cart li[data-code="N39.0"]')).to_have_count(1)
    # 人工關聯：病原碼；家族碼：N39 類目
    expect(page.locator('#related .chip[data-code="B96.20"]')).to_have_count(1)
    # 連鎖：點相關碼也進清單並更新推薦
    page.locator('#related .chip[data-code="B96.20"]').click()
    expect(page.locator('#cart li[data-code="B96.20"]')).to_have_count(1)

def test_duplicate_not_added(page):
    reset(page)
    page.locator('.chip[data-code="I10"]').first.click()
    page.locator('.chip[data-code="I10"]').first.click()
    expect(page.locator("#cart li")).to_have_count(1)

def test_mode_switch(page):
    reset(page)
    page.click("#mode-surg")
    expect(page.locator("#panels-title")).to_contain_text("外科")
    expect(page.locator('#quick .chip[data-code="Z48.02"]')).to_have_count(1)
    page.click("#mode-im")
    expect(page.locator("#panels-title")).to_contain_text("內科")

def test_copy_formats(page):
    reset(page)
    page.locator('.chip[data-code="I10"]').first.click()
    page.locator('.chip[data-code="E11.9"]').first.click()
    page.click("#copy-lines")
    assert page.evaluate("navigator.clipboard.readText()") == "I10\nE11.9"
    page.click("#copy-comma")
    assert page.evaluate("navigator.clipboard.readText()") == "I10,E11.9"
    page.click("#copy-names")
    text = page.evaluate("navigator.clipboard.readText()")
    assert text.startswith("I10\t") and "E11.9\t" in text

def test_remove_and_clear(page):
    reset(page)
    page.locator('.chip[data-code="I10"]').first.click()
    page.locator('.chip[data-code="E11.9"]').first.click()
    page.locator('#cart li[data-code="I10"] button').click()
    expect(page.locator("#cart li")).to_have_count(1)
    page.click("#clear-cart")
    expect(page.locator("#cart li")).to_have_count(0)

def test_reload_clears_cart(page, page_url):
    page.locator('.chip[data-code="I10"]').first.click()
    page.reload()
    page.wait_for_selector('body[data-ready="1"]')
    expect(page.locator("#cart li")).to_have_count(0)
```

- [ ] **Step 3: 執行全部 E2E**

Run: `python -m pytest tests/e2e_test.py -v`
Expected: 10 PASS。失敗時逐一修 `src/`（改後必須重跑 `python build/build.py` 再測——E2E 測的是 dist）。

- [ ] **Step 4: 全套測試最終確認**

Run: `python -m pytest tests/ -v && node --test tests/logic.test.mjs`
Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add tests/e2e_test.py dist/icd10.html
git commit -m "test: Playwright E2E 全流程（含剪貼簿與效能）"
```

---

### Task 12: README 與 GitHub 發布

**Files:**
- Create: `README.md`

- [ ] **Step 1: 寫 README.md**

```markdown
# ICD-10 門診導引

單一離線 HTML 的 ICD-10-CM 選碼工具，為門診情境設計：症狀導向面板、內科／外科模式、
慢性病與感染科常用快選、相關碼連鎖推薦、就診清單一鍵複製、全庫即時搜尋。

## 使用

直接用 Edge / Chrome（80+）開啟 [`dist/icd10.html`](dist/icd10.html)，免安裝、免網路。

- **搜尋**：中文（蜂窩）、英文（cellulitis）、代碼前綴（L03 或 E119）皆可。
- **點任何代碼**加入右側「本次就診清單」，同時跳出相關代碼（臨床關聯＋同類目）供連鎖加選。
- **複製**：每行一碼／逗號分隔／碼＋名稱 三種格式。
- 灰色代碼為類目碼（不可申報），不能加入清單。

## 開發

```bash
python build/fetch_data.py   # 下載健保署 CSV（13.7MB）
python build/convert.py      # 轉 data/codes.min.json
python build/build.py        # 組裝 dist/icd10.html
python -m pytest tests/ -v   # Python 測試（資料完整性/精選碼驗證/建置/E2E）
node --test tests/logic.test.mjs
```

## 資料來源與授權

- 診斷碼資料：衛福部健保署「2023年版中文版 ICD-10-CM」，取自
  [政府資料開放平臺 dataset 177507](https://data.gov.tw/dataset/177507)，
  依《政府資料開放授權條款》使用。
- 精選面板與關聯表為人工整理，僅供快速選碼參考；申報前請依臨床實況確認。

## 免責

本工具僅輔助選碼，不構成醫療或申報建議；最終診斷碼以醫師判斷與健保署現行規範為準。
```

- [ ] **Step 2: 發布 GitHub**

Run:
```bash
gh repo create ICD10 --public --source . --push
```
Expected: repo 建立且 main 推送成功（`gh repo view ICD10 --json url` 可確認）

- [ ] **Step 3: Commit README（若 Step 2 前尚未 commit）**

```bash
git add README.md
git commit -m "docs: README（使用/開發/資料授權）"
git push
```

- [ ] **Step 4: 使用者驗收提醒**

回報使用者時附上：(1) `dist/icd10.html` 檔案位置與開啟方式 (2) 精選內容（面板/快選/關聯表）屬臨床判斷成分，請使用者以感染科視角抽查其正確性與實用性——這是機器驗證覆蓋不到的部分。

---

## Self-Review 紀錄

- **Spec 覆蓋**：症狀導向面板(T6)、內外科切換(T7/T9)、慢病+感染快選(T5)、相關碼兩層推薦(T8/T9)、累積清單三格式複製(T4/T9)、全庫搜尋含非葉碼處理(T4/T9)、單檔離線與錯誤處理(T9/T10)、資料完整性與 E2E(T3/T11)、GitHub 交付(T12)——設計文件各節皆有對應任務。
- **型別/介面一致性**：`[code,use,en,zh]` 貫穿 T2→T4→T9；CURATED 鍵名 T5–T8 產出與 T10 映射、T9 消費一致；DOM 約定 T9 產出、T11 消費一致。
- **已知風險**：精選碼為記憶撰寫，機率性有錯——T5–T8 每個任務都有機器驗證關卡（存在＋USE=1）擋下不存在的碼；臨床適切性由使用者驗收（T12 Step 4）。
