# 剪貼簿輸出設定實作計畫

依使用者已核准的設計，在本次工作階段以 executing-plans 逐項完成，不另開工作樹或提交既有未提交內容。

目標：五種輸出皆可編輯範本，所有版面與 PiP 共用設定與格式化。

架構：新增純函式 clipboard-format.js 負責欄位、預設、驗證與格式化；state.js 保存偏好；clipboard-settings.js 提供設定內頁；render-shared.js 建立計算機資料並維持既有預設文字；所有複製入口使用同一格式化出口。

- [x] 純函式測試先行：日期、單碼、清單、CCr、Lipid；未知欄位與括號錯誤、空值、非執行式文字、原始預設相容。執行 `node --test tests/clipboard-format.test.mjs` 確認先失敗再通過。
- [x] 保存測試先行：只存範本、不存個案；無效設定回復預設並提示；還原單類不清掉其他類；重開保留。接上 state.js 與 build/build.py 模組順序。
- [x] 設定內頁：類別選單、預設格式、可點擊欄位、整體範本、清單單筆／分隔、Lipid 段落範本、範例預覽、驗證訊息、儲存與還原。以本地事件委派確保整個設定頁搬入 PiP 仍可操作。
- [x] 接線：日期主視窗／PiP、清單自動複製與 HIS 預覽、單碼滑鼠／鍵盤、Lipid、CCr。設定更新只重繪預覽，不在編輯或儲存時意外覆寫剪貼簿。
- [x] E2E：三種版面設定保存、插入欄位、驗證錯誤、重載、五類實際複製、既有格式相容、PiP 與 176px 無橫向溢出。
- [x] 執行 JS 全套及相關 Python／E2E，視覺抽查，更新使用說明，重新建置與診間打包，核對包內 HTML。

驗證：177 項 JavaScript、48 項相關 E2E、61 項建置與資料檢查通過；獨立審查修正 3 項相容性問題，診間包內容已核對。
