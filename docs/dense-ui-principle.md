<!-- 2026-09-17: 適用範圍延伸到工作台（1a）；實測工作台是三版面裡捲動最長、空白最多的，見 docs/superpowers/specs/2026-09-17-usability-overhaul-design.md -->
<!-- 2026-09-17: 加「第四筆：工作台」一節——階段 1～3 的實測數字（面板索引、清單兩行、header 統一尺寸與顏色語意、慢病浮層加寬），以及「工作台常見疾病仍不收合」的理由 -->

# 版面密度原則：在有限的版面塞入最大量的資訊

> 使用者原話（2026-08-11，針對側掛窄欄的 header）：
>
> 「我希望這些按鈕只佔一列就好　門急診外科的按鈕不用這麼寬　緊縮跟文字一樣寬就好
> **這個介面的中心原則就是在有限的版面塞入最大量的資訊**」

這是產品原則，不是一次性的修正要求。**改動工作台（1a）、側掛窄欄（1c）與手機（1b）
版面時，以本檔為準；與其他設計參考（含 `.review/design-ref/` 的交付稿）衝突時，本檔優先。**

## 為什麼這條原則對這個產品特別重要

這支工具的使用情境是**看診中**：側掛窄欄貼在 HIS 旁邊，寬度只有 176〜600px，
醫師一邊問診一邊掃視。版面上每多一列 chrome（控制項、留白、看過就沒用的提示），
就少一列**診斷碼**——也就是少一個「不用打字、不用搜尋、直接點」的機會。
捲動一次的成本在門診是很高的，所以「省下 30px」不是美學問題，是動線問題。

## 原則

**版面預算優先給內容（診斷碼、主訴、部位），不給控制項與留白。**
任何佔用固定高度的東西都要能回答：它每一次看診都被用到嗎？沒有它會怎樣？

## 硬性邊界：不得為了塞而犧牲

### 一項資訊一行，直接換行，不用分號串接（2026-09-07）

此規則適用於所有分頁、計算機、彈窗，以及工作台、側掛窄欄與手機版面。
多個可獨立閱讀的結果、條件、劑量步驟或操作提示，應各自分行；不可用分號或直線分隔符擠在同一行，也不能只依賴窄視窗自動折行。

- 例如 BSA 與校正 CCr 各一行；血脂起始門檻、主要目標、次要目標各一行；不同學名的彙總各一行。
- 使用區塊元素，或明確換行搭配 `white-space: pre-line`。維持緊湊行距，不增加無用的大段留白。
- 劑量、適用條件、單位、先後順序與「或／且」關係必須完整保留；括號內的補充不任意拆開。來源原文與資料檔保留原貌，在呈現層處理分行。
- 複製結果若包含並列項目，也以換行區分。新增或修改介面時，需檢查同類分頁，並實際驗證窄欄可讀性及無橫向溢出。

以下既有的觸控、文字與臨床分層邊界同樣適用。

CCr 抗菌藥速查（2026-09-07）：提供前往選藥／返回輸入捷徑；CCr 關閉列隨捲動固定，結果重述本次 CCr 與計算體重。340px 的藥物標籤與欄位併列，176px 採上下排，避免長學名與方案文字擠到無法辨識。完整腎功能表預設收起，先看本次符合的劑量；藥物特殊單位、loading 與限制不因壓縮而刪除。手機控制項維持至少 44px。

以下四條**優先於密度**。壓縮到踩線就停手，改壓別的地方。

1. **手機觸控目標 ≥ 44px。** 那是手指的物理尺寸，不是風格選擇。
   `mobile.css` 的 44px（部位 pill、面板展開鈕、清單列小鈕、搜尋框、設定鈕）一律不准調小。
   側掛窄欄是滑鼠操作，不受這條約束，但仍不宜低於 20px 高。
2. **文字不可小到看不清。** 本專案的下限：主要內容（診斷碼、中文名）≥ 11px；
   次要 chrome（模式鈕、提示）≥ 10px。低於 10px 一律不准。
   手機搜尋框另有 16px 下限（低於 16px 會讓行動瀏覽器聚焦時自動放大整頁）。
3. **臨床安全的視覺分層不得被壓到看不出來。** 具體是三項：
   急診紅旗（`.chip--warn` 的警示色下緣）、不可申報的類目碼（`.chip.cat` 的虛線與
   `aria-disabled`）、附加碼標記（`.chip-tag` 與清單頂端的主診斷警示）。
   這三項的顏色、邊框、標記文字不因密度調整而弱化或移除。
4. **藏起可見文字時，可讀名稱必須有替代來源。** 只留 icon 的控制項一定要有能自我說明的
   `title`（且隨狀態更新），狀態另有 `aria-pressed` 之類的第二條線索。
   **砍文字是最後手段**：要先試過壓內距、壓字級、壓 letter-spacing，
   而且要在程式碼註解裡留下「為什麼非砍不可」的寬度算式（見 `dock.css` 的 media query）。

## 換到密度的手法（依「先做哪個」排序）

1. **控制列能合併就合併。** 一列 chrome 的固定成本是內容高度＋間距，通常 26〜30px。
   例：模式三鈕原本自己佔一行，與置頂／設定併成同一列，header 97px → 67px。
2. **控制項收成內容寬，不要等寬撐滿。** `flex: 1` 撐滿是預設值不是設計決定；
   低頻控制項（一次看診設定一次的模式切換）不需要大靶。多出來的寬度交給彈性 spacer。
3. **單行不換行的 UI 骨架收 `line-height`。** 全域 body 是 `line-height: 1.55`（給內文的），
   套在 `white-space: nowrap` 的按鈕與標題上時，那 0.55 完全是留白。
   1c 的部位鈕、部位標題、面板標題一律收到 1.2〜1.25。**會換行的內文不要動**——
   那裡的行距是可讀性。
4. **一次性提示要自己消失。** 「已開啟置頂小視窗，可拖到 HIS 旁邊」是操作說明，
   看過就沒用，卻在 176px 下佔兩行約 27px。成功類提示設逾時自動收掉；
   **失敗／未解決的提示相反，永遠留著**（它講的是使用者下一步該做什麼）。
5. **內距對齊到版面基準，不要一區一個數字。** 1c 的基準是 5〜6px；
   清單摘要列原本 8/10px，比鄰居鬆卻沒有分隔作用（分隔靠上緣線），收齊即省 6px。
6. **位置用百分比／`calc()`，不要寫死魔術數字。** 設定 popover 的 `top` 從 93px 改成 `100%`
   （貼著 `position: relative` 的 header 下緣）：header 少一列、或提示列讓 header 長高，
   都不必回頭改這個數字。寫死的數字每次改版都會失準一次，而失準的後果是
   popover 蓋住開它的那顆鈕、使用者關不掉。1b 早就是這個寫法。

## 已套用的決策與量到的數字（2026-08-11，1c 側掛窄欄）

Playwright 實測，`dist/icd10.html`，日／夜 × 三模式；量測腳本產出在 `.review/r7-shots/`。

| 改動 | 176px | 565px（使用者實際的 PiP 寬） |
| --- | --- | --- |
| header 三列 → 兩列（模式鈕＋置頂＋設定同列，模式鈕收成文字寬） | 97 → 67px | 97 → 67px |
| 部位鈕／部位標題／面板標題收 `line-height` | 部位區 166.3 → 148px | 88.1 → 79px |
| 清單摘要列內距 8/10 → 5/6 | 清單區 80.1 → 74.1px | 同左 |
| **可視內容區（`.dock-scroll`）淨增** | **549.6 → 603.9px（＋54.3，＋9.9%）** | **627.7 → 672.9px（＋45.2，＋7.2%）** |
| 置頂成功提示改為逾時自動收掉 | 置頂後另省約 27px（兩行） | 約 14px（一行） |

模式鈕從等寬撐滿（每顆 53〜54px、整列 164px）收成文字寬（每顆 28〜29px、整列 89.2px）。

## 刻意**花掉**密度的一筆：慢病速查「一條一行」（2026-08-26）

使用者原話：「出現『；』分號這種　看起來就是第二個句子或是另一個適用條例　
這種是否換行或分段會比較好閱讀」。條文改成依句號＋分號斷段、掛項目符號與懸掛縮排
（`.chronic-seg`）。這是**用版面換可讀性**，方向與本檔相反，所以要有數字。

同一天的第二輪：使用者再指出「Fibrate：第一列黏在抬頭上、第二列變孤兒」，
於是主文的短標提成獨立的**抬頭行**（`.chronic-seg.is-head`），底下條件全部平行掛——
多一列的成本就是下表兩個數字的差（收合 2556→2597）。

Playwright 量測 `#chronic-body` 的 `scrollHeight`，LIPID 主題（21 條）：

| 版面 | 收合（預設狀態） | 全部補充攤開 |
| --- | --- | --- |
| 1c 176px | 2544 → 2597（**+53，+2.1%**） | 9090 → 10089（+999，+11.0%） |
| 1c 565px | 1306 → 1411（+105，+8.0%） | 3710 → 4215（+505，+13.6%） |
| 1b 手機 390px | 2507 → 2567（+60，+2.4%） | 6454 → 6900（+446，+6.9%） |
| 1a 桌機 1280px | 1467 → 1590（+123，+8.4%） | 4039 → 4567（+528，+13.1%） |

**為什麼 176px 收得下**：那 14 條含分號的主文本來就會折行，在分號處斷開多半只是把
折行位置換個地方；抬頭行也只發生在 6 條有短標的條目上。真正的成本落在「補充攤開」
——但那是使用者主動點開、要逐條讀的狀態，正是可讀性該優先於密度的地方。

懸掛縮排 `.8em`（176px 下約 8.8px）也是刻意的最小值：夠讓續行縮進去看得出來，
又不至於在最窄欄吃掉一個字的寬度。**單段的條目不掛符號也不縮排**，所以 51/65 條
完全維持原樣。三套版面 × 日夜主題皆無水平溢出（`test_chronic_panel_never_overflows_at_176`）。

## 第二筆：對回官方原文重寫，版面又翻了一倍（2026-08-26）

使用者要求「重讀官方文件後用類似方式重寫」。64 條逐條對回官方原文之後，把原本只躺在
「補充」、但實際屬於**能不能開**的門檻拉上主文（例：SGLT2i 心衰竭補上 LVEF 41–49% 路徑、
PCSK9 續用補上 HoFH 那組不同的數字、表二補上危險因子分級）。

同一組量法，LIPID 主題（21 條）從**改版前的原始基準**到現在：

| 版面 | 收合（預設狀態） | 全部補充攤開 |
| --- | --- | --- |
| 1c 176px | 2544 → 5070（**+99%**） | 9090 → 13000（+43%） |
| 1c 565px | 1306 → 2729（+109%） | 3710 → 5664（+53%） |
| 1b 手機 390px | 2507 → 4364（+74%） | 6454 → 8942（+39%） |
| 1a 桌機 1280px | 1467 → 3158（+115%） | 4039 → 6283（+56%） |

**這一倍幾乎全部是內容，不是排版。** 純排版（分號斷行＋抬頭行）在 176px 收合狀態只佔
+2.1%（2544→2597，見上一節）；其餘是主文多載了門檻條件。

**為什麼判斷值得**：這個面板存在的理由就是回答「能不能開」。門檻藏在收合的補充裡，
等於醫師掃主文會拿到不完整的判準——LVEF 45% 的病人原本會被主文誤判成不給付。
把答案放在會被看到的地方，比省版面重要。

**評估過「回收版面」，結論是不划算**：逐條看完 64 項，真正可砍的只有 5 段
（「換句話重講」與「指向隔壁條目」，例如 #13 與 #15 互相重複的那句、
「指引各表的 office BP 一律指標準化診間血壓」這種讀指引的方法）。已經砍掉，
但那只值約 3% 的高度，而每多砍一段就多一分誤刪門檻的風險。**其餘保留。**

**改用不花版面的方式解決捲動痛點**：分段標題（能不能開／怎麼開／別踩雷／不給付／治療目標）
改成 `position: sticky`。捲長一格沒少，但「捲到一半不知道自己在哪一段」消失了——
sticky 重複利用的是本來就存在的那一列，量測前後 `scrollHeight` 完全相同。
醫師開這個面板時心裡有個特定問題，標題常駐才對得上那個問題。

實作兩個細節（都實測過）：sticky 元素**必須給不透明背景**，否則捲過去會有兩層字疊在一起；
「別踩雷」「不給付」兩段有左邊界線與內距，標題要 `margin-left: -8px; padding-left: 8px`
把背景拉滿那塊內距，但**保留那 2px 邊界線**——它是該段的身分標記。

三套版面 × 日夜主題仍無水平溢出。

## 第三筆：入口三鈕併成一顆、CCr 搬進同一排（2026-09-01）

使用者要求「DM／HTN／LIPID 整合成『健保規範條文』一顆」、「血脂改叫血脂計算機、
CCr 放到它右邊」。做完之後那一排是 **健保規範條文｜血脂計算機｜CCr**。

量測條件同上（Playwright，1c 側掛窄欄，176／565px × 日夜；日夜同值故只列一組）。

| 量的東西 | 176px 改前 → 改後 | 565px 改前 → 改後 |
| --- | --- | --- |
| `.dock-head`（固定 chrome） | 92 → **92**（不變） | 67 → **67**（不變） |
| `#chronic-switch`（在捲動區） | 33 → **58**（+25，折成兩列） | 33 → **33**（不變） |
| `.dock-scroll` clientHeight | 686 → 686 | 734 → 734 |
| `.dock-tools` 需要的寬度／可用 | 249.3 → 218.1／163 | 249.3 → 218.1／327 |

**這一筆是淨支出，不是節省。** 我原本在註解裡寫「CCr 搬走，header 的寬度帳就鬆開了」，
量了才發現不成立：`.dock-tools` 在 176px 需要 249.3px、可用只有 163，本來就折成兩列；
搬掉 CCr 後 218.1 仍然折，`.dock-head` 一樣是 92px。真正省到的只有
**231–262px 這一段 dock 寬度**（在這區間內原本折兩列、現在一列），
而使用者實際用的 340／565px 兩個寬度本來就不折——**在他的機器上是零收益**。

代價則是實的：176px 下三顆放不進一列（79.3 ＋ 70.1 ＋ 28.2 ＝ 178 ＞ 163），
`.chronic-switch` 開 `flex-wrap` 折成兩列，那一排 33→58px。它在捲動區裡，
永久成本仍是 0（往下選碼時就捲走了），但**第一屏少了 25px 的代碼**。

**為什麼還是做**：這是使用者指定的整併，不是密度優化——把它記在這裡是為了防止日後
有人翻到那幾行註解，以為「搬 CCr」是一個省版面的手法而照抄。真正的收益是動線
（兩個計算機放一起只要記一個位置），密度上它要付 25px。

另外，同一輪抓到一個**既有**的觸控門檻破口：`#lipid-btn` 帶著 `.seg-btn--sm`
（那是給 176px 的 22px／10px），手機版沒有蓋掉它，實測只有 22px 高、10px 字，
遠低於 44px 硬性邊界。它從 2026-08-26 加入時就是壞的，因為當時的手機 E2E 只量
`.chronic-btn`。現在 `mobile.css` 補了規則，測試也改成逐顆量這一排的三個 id。

## 第四筆：工作台（2026-09-17）

本檔的適用範圍延伸到 1a 之後的第一輪。UX 稽核的結論是「密度最差、捲動最長、空白最多的
正是工作台」，所以這一輪做的事有兩類：**把已經浪費掉的版面換成動線**（不花新預算），
以及**把攜帶錯誤語意的視覺差異拿掉**（不花版面）。

量測條件：Playwright，`dist/icd10.html`，1a 1440×900、1c 176／340px、1b 390×844，
日／夜兩套主題（數值相同的只列一組）。

### 階段 1、2 已量過的（面板索引、清單兩行、通知列、返回）

| 量的東西 | 改前 → 改後 |
| --- | --- |
| 1a `.rail-col` 底部死空間 | 332.7 → **0px**（放面板索引） |
| 1a `#panels` scrollHeight（門診第一部位／全部部位） | 5249 → 5249／27034 → **27034**（索引不動內容） |
| 通知列顯示時 `.dock-scroll`／`#panels` clientHeight | 不變（絕對定位覆蓋，不推擠正要點的碼） |
| 1a `#cart` 三筆 | 131.4 → **220.5px**（中文名獨立第二行） |
| 1c 176px `#cart` 三筆 | 150.9 → 172.7px，被 120px 上限吸收；`.dock-scroll` 1344 → **1344** |
| 1b `#m-scroll`（抽屜展開時） | 227 → **181px**（−46） |

`#cart` 那 89px 是**刻意花掉的**：中文名是貼進 HIS 前唯一的核對機會，而 1440px 的
工作台原本只給它 136px（比 340px 窄欄的 327px 還少一半），三筆測試碼全部截斷。
1b 的 −46px 只發生在抽屜主動展開的狀態，不是常態版面。

### 這一輪（header 與顏色語意）

| 量的東西 | 改前 → 改後 |
| --- | --- |
| 1a `.app-header` 高度 | 63 → **55px**（−8） |
| 1a header 控制項高度 | 32／40／28／40 四種 → **一律 32** |
| 1a header 控制項字級 | 14／13／12／16 四種 → **一律 13** |
| 1a 搜尋框寬度（1920／1440／1100／900） | 1118／638／358／158 → 1110／630／351／**151px** |
| 1b `.m-header` 高度 | 119 → **119**（日期鈕搬家不付高度） |
| 1b `.m-mode-row` 寬度分配 | 日期 59.1 ＋ 模式 294.9（97／98／98）→ **模式 362**（119.3／120.3／120.3） |
| 1b `.m-head-row` 寬度分配 | 搜尋 306 ＋ 設定 48 → 搜尋 240.9 ＋ 日期 55.1 ＋ 設定 50 |
| 1a `#chronic-panel` 寬度 | 720 → **1100px** |
| 1a `#chronic-body` 捲動屏數（DM／HTN／LIPID） | 3.73／4.16／7.26 → 3.71／4.08／**6.74** |
| 1a 浮層內被裁掉的出處條號（LIPID） | 7 → **0 條** |
| 1a `#clear-cart` 對比（日／夜） | 3.81／7.86 → **5.94**／10.65 |
| 1b `#clear-cart` 對比（日／夜） | 3.71／8.97 → **5.78**／12.15（1c 6.76，本來就過，未動） |
| 1c `.dock-cart-codes` 字級／`.dock-cart-head` 高度 | 13 → **12px**／35.1 → **35.0px** |
| 1b「設定」字級 | 13 → **14px** |

**header 矮了 8px，而且是往下收不是重排（寬度另外付了 8px，見下一段）。** 原本的四種高度不是層級設計，
是四個來源湊在一起：日期吃 `.btn` 預設、模式鈕跟搜尋框對齊 40、三顆工具鈕另外做小成 28
「免得被當成第四種模式」。結果是**大小與使用頻率相反**——最常用的臨床查詢
（健保規範條文）全列最小，一診只按一次的「側掛置頂」最大。統一之後尺寸不再攜帶語意，
層級改由顏色表達：淺藍（`--his-btn`）只給**按下去會寫剪貼簿**的鈕（日期、CCr 複製、
Lipid 複製），其餘一律透明外框。`#ccr-btn` 因此掉了淺藍——它只是開一個浮層，
而同排語意相同的另外兩顆本來就是透明的。

**搜尋框那一格要收 `line-height`。** 字級 16 → 13 之後，body 的 1.55 讓自然高度變成
34.2px，**壓過** `min-height: 32px` 把整列撐高 2px。收到 1.2（手法 #3：單行不換行的
UI 骨架）自然高度 29.6px，min-height 才真的說了算。

**收掉的高度不會變成寬度：搜尋框每個寬度各少 8px。** 三顆工具鈕的字級 12 → 13 讓它們
各寬了幾 px，而 header 是 nowrap、搜尋框是唯一 `flex: 1` 的元素，所以全部從它身上扣
（900px 下 158 → 151px，仍不水平溢出，`≤1239px 收成圖示` 的行為原樣保留）。
這筆是刻意付的：那三顆是最常用的臨床查詢。順帶更正 `wide.css` 註解裡一組與實測不符的
舊數字（原寫 1100→299、900→99px）。

**手機日期鈕是搬家，不是縮小。** 它原本在模式分段列裡，填滿的淺藍與深藍的「已選中」
並排，看起來就是第四個模式；按下去卻是複製今天的日期。搬到搜尋列與「設定」同側之後
模式三鈕各多 22px 寬，而 `.m-header` 前後同為 119px——兩列本來就都是 44px，
多一顆是跟 `flex: 1` 的搜尋框借寬度（306 → 240.9px，搜尋狀態下再讓給「返回」剩 178.9px，
兩種狀態都不折行）。44px 與 16px 兩條硬性邊界一格都沒有動。

**側掛摘要那 0.1px 是測試抓出來的，不是預期內的。** 原本的註解（與我動手前的判斷）
都寫著「這一列的高度由『清空』的 26px 決定，降字級不會動到它」——實測是錯的：
13px 的摘要加上 6px 內距是 26.1px，比「清空」高 0.1px，本來就是它在決定高度。
`test_e2e_navigation.py` 的等式基準因此紅燈，值就是這麼被抓到的。
**寫著「不會動到」的註解也要量一次**；等式基準比 `<=` 貴 0.1px 的維護成本，
換到的正是這種「以為沒差其實有差」的回報。

**「清空」的對比不只 1a 壞掉。** 稽核只記了工作台一筆（3.81:1），實測 1b 同樣是 3.71:1
——它沒被掃到是因為量的當下清單抽屜收合著、那顆鈕是 0×0，不算「可見元素」。
兩邊是同一個缺陷（`.btn-ghost` 的 `--color-accent` 當文字色坐在淺灰底上），所以修法
也是同一條不帶版面前綴的 `#clear-cart`。**「稽核沒列到」不等於「那裡沒事」**：
掃描器看不見的狀態要自己補量。

**慢病速查浮層這一筆要老實記：它幾乎沒有省到捲動。** 稽核建議是「同一份內容少捲一半」，
實測不成立——720 → 1100px 之後 DM 2867 → 2846（−0.7%）、HTN 3193 → 3130（−2.0%）、
LIPID 5579 → 5175（−7.2%）。原因是這個面板的內容多半是本來就排得下一行的條文句，
多出來的 380px 大部分變成每行尾端的空白。**真正買到的是另一件事**：`.chronic-source`
（出處條號）是單行不換行的，686px 的內文寬度下 LIPID 主題有 7 條被裁掉看不完整，
1066px 下是 0 條。查給付規定時「出自哪一號公告」不能只看得到前半句，所以這一筆值得做，
但**理由是可讀性不是密度**，日後不要拿它當「加寬可以省捲動」的先例。

驗證在 `tests/test_e2e_visual_semantics.py`（18 個案例，三版面 × 日夜）：
header 全列同高同字級且 `.app-header` ≤ 63px、三顆工具鈕同底色、淺藍只在會寫剪貼簿的鈕上、
手機日期鈕不在模式列且與搜尋框同列、`#clear-cart` 日夜都過 AA、浮層 ≥ 1090px 且零裁切。
`tests/test_e2e_navigation.py` 的 `BASELINE["wide_header"]` 同步從 63.0 收到 55.0——
維持等式而不是放寬成 `<=`，才抓得到「下次有人把某顆鈕改回 40px」。

## 刻意**沒有**做的事（附理由，避免日後被當成漏掉）

- **把「全部」鈕併進部位格線當第一格。**
  量過才知道沒有效益：176px 是兩欄，三種模式的部位數都是偶數（門診／外科 10、急診 8），
  併進去之後總格數變奇數，最後一格空著，**列數完全不變、省 0px**。
  565px 下才省得到一列（23px），但要付出「全部」被當成其中一個部位的語意代價
  （見 `dock-design-deviations.md`）。有效益的寬度換不到值得的代價，故不動。
  日後若部位數變成奇數，這筆帳要重算。
- **壓縮手機版面的幾何。** 1b 的高度幾乎全部由 44px 觸控目標決定，能動的只有
  header／部位列的內距（合計約 10px，佔 844px 螢幕的 1.2%），
  卻要冒動到 971 行手機 E2E 的風險。手機要提升密度應該從「一屏能放幾個碼」下手
  （內容區的列高與分組），不是從 chrome 的內距。
- **再壓搜尋框（28px）與「複製並貼入 HIS」（30px）。**
  這兩個是主要輸入與主要行動，各只有一份，壓下去換到的像素少、誤觸成本高。
- **把工作台的常見疾病改成可收合。**（2026-09-17）
  1a 的面板預設全部展開，所以 `#panels` 光是門診第一部位就要捲 5249px、
  取消部位選取更是 27034px——看起來是收合的完美適用情境，但那個「全部展開」
  是 2026-09-01 使用者明確要求的（他要一眼掃完，不要每個面板再點一次）。
  **使用者指定的行為不因為量到一個大數字就翻掉。** 替代方案是左欄的面板索引：
  它吃的是本來就空著的 332.7px，捲動長度一格沒少（27034 → 27034），
  但「要到第 6 個面板只能一路捲」這件事消失了。1c 與 1b 的收合維持原樣，
  那兩個版面的寬度帳與 1a 完全不同。

## 怎麼驗證（改這塊之前先看這裡）

- `tests/test_e2e_dock.py::test_header_controls_share_one_row`
  ——五個控制項同列、模式鈕是文字寬、不溢出、文字不被裁切、header ≤ 72px。
- `tests/test_e2e_dock.py::test_only_pin_and_expand_labels_are_dropped_and_only_when_narrowest`
  ——176px 只有「置頂」與「展開」的文字被藏起（`aria-label` 與 `title` 補上），
  模式鈕與「設定」的文字任何寬度都在；565px 文字必須回來。
- `tests/test_e2e_dock.py::test_pin_note_success_auto_dismisses_failure_stays`
  ——成功提示會自己消失、失敗提示不會。
- `tests/test_e2e_dock.py::test_no_horizontal_overflow`（日／夜 × 三模式）與
  `test_settings_popover_fits_and_switches_mode`（popover 不得蓋住開它的鈕）。

新的密度調整**一律要有對應的量測數字**（改前／改後、176 與 565 兩個寬度、日夜兩套主題），
不接受「看起來比較緊」。

## 側掛選碼動線（2026-09-07）

使用者指出找碼、切部位與加選需要反覆捲動。這次保留原字級、部位排列與代碼內容，
優先處理搜尋結果在可視區外、部位切換丟失位置，以及下方區塊擠掉正在選的碼。

Playwright、900px 高、門診全部部位、初始收合疾病、加入第一筆 I10；日／夜兩套量測相同。
565px 量測把 `--dock-w` 設為 565px，避免主視窗的預設 340px 上限混入結果。

| 項目 | 176px 改前 → 改後 | 340px 改前 → 改後 | 565px 改前 → 改後 |
| --- | --- | --- | --- |
| 未選碼時內容區高度 | 686 → 686px | 734 → 734px | 757 → 757px |
| 加入 I10 後內容區高度 | 428 → 501px | 476 → 549px | 499 → 572px |
| 加入 I10 後完整可見代碼數 | 10 → 13 | 16 → 19 | 21 → 24 |
| 全部部位捲動長度 | 6548 → 6582px | 5011 → 5045px | 4255 → 4289px |

主要收益來自相關建議的預設高度 170→96px；新增的收合控制沿用標題列，扣除標題高度差後，
實際多出 73px。清單高度上限另從 210→120px，只有多碼時才會碰到這個上限；手動設定高度仍優先。
相關建議收合後保留標題和數量、隱藏分隔條；不會因下一次加碼而自行展開。

代價是面板標題略增高，讓展開鈕有 20px 操作高度；全部部位合計多 34px 捲動長度，
未選碼時第一屏的完整代碼數不變。標題整列可點、捲動時黏在內容區上緣，因此收合不必回頭找小按鈕。

搜尋另有自己的顯示狀態：結果取代面板、從頂端開始，返回才還原原部位位置。
已選碼的勾號不取代紅旗配色、類目虛線或附加碼標記。
操作回歸案例在 `tests/test_e2e_dock_flow.py`；前後截圖與量測在 `.review/dock-flow/`。
