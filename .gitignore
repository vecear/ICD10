data/
__pycache__/
.pytest_cache/
node_modules/
*.pyc
.review/
.superpowers/
# 帶去診間用的 AutoHotkey 執行檔／安裝檔：第三方二進位，不進版控
tools/*.exe
tools/*.zip
# 診間包：由 dist 與 tools 的檔案組出來的拷貝，不是原始碼
診間包/
診間包*.zip
# 為了繞過 Gmail 封鎖執行檔而做的打包嘗試（改副檔名／分割壓縮）。
# 都是產物不是原始碼；正式做法是 tools/README.md 寫的 certutil base64。
tools/Au.*
