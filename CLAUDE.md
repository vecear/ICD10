# ICD-10 門診導引

實際在診間使用的工具。改動之前先讀 `README.md`（架構與版面）與
`docs/dense-ui-principle.md`（密度原則）。

## 交付流程：改完一定要重新打包（最容易漏，後果最痛）

診間電腦**只能收信**——不能上網下載、不能插隨身碟、不能用雲端硬碟。
使用者的作業方式是「整個診間包寄過去、解壓覆蓋」，**不會去挑哪個檔有更新**。

所以只要動到下面任何一個，收尾一定要重新打包：

- `dist/icd10.html`（改了 `src/` 就會變）
- `tools/his-paste.ahk`
- `tools/診間使用說明.txt`

```bash
python build/build.py && python tools/pack_for_clinic.py
```

漏掉這一步，他寄到診間的就是舊版，而且**要到診間才會發現**——那裡補不了檔，
得等下一次門診。這不是整潔問題，是他當診用不到新功能。

打包會自動擋三種診間發現不了的錯誤（任一不過就中止）：

1. base64 還原後的 SHA-256 與原始 exe 不符
2. zip 內含會被郵件封鎖的副檔名（Gmail 連壓縮檔裡的 exe 都擋，看的是內容標頭）
3. `tools/診間使用說明.txt` 教的「改第 N 行」與 `his-paste.ahk` 實際行號不一致，
   或還在教人按已經移除的按鈕

改了 `his-paste.ahk` 的設定區行號、或改了 UI 上的按鈕，記得同步更新說明——
第 3 項會抓，但抓到時是**打包中止**，不是自動修好。

## 驗證

```bash
python -m pytest -q          # 含三套版面的 E2E
node --test tests/*.test.mjs # 純邏輯單元測試
```

UI 改動一律要實跑驗證過才算完成（專案慣例，見 `docs/`）。
資料改動另有臨床不變量測試（`tests/test_clinical_invariants.py`），
最容易踩到的是「同一代碼在門診與急診必須是同一個標籤字」。

**AutoHotkey 不必每次測**：除非動到 `tools/his-paste.ahk`，否則不用做 AHK 的跨進程實測
（那要開真的 HIS 視窗，成本高而且與網頁改動無關）。注意這不是「不用打包」：
打包仍然每次都要跑，它內建的說明書行號檢查是自動的，不算手動測試。

## 診間工具

`tools/` 下是給診間電腦用的東西（AHK 熱鍵貼入、打包腳本），
用法與已知限制見 `tools/README.md`。
